# קימפול Tik-Nick לקובץ EXE עצמאי

## מה זה נותן
קובץ `TikNick.exe` יחיד שרץ על כל מחשב Windows 10/11 **בלי**:
- התקנת Python
- התקנת חבילות (pip)
- חיבור לאינטרנט (מלבד פיצ'רים שדורשים רשת בכוונה)

## איך מקמפלים

### הדרך הקלה
לחץ פעמיים על **`build.bat`** וחכה. בסיום, ה-EXE יהיה ב:
```
dist\TikNick.exe
```

### ידנית (אם מעדיפים)
פתח שורת פקודה בתיקייה והרץ:
```
pip install pyinstaller pywebview
pyinstaller --noconfirm --clean TikNick.spec
```

## הפצה
העתק את **`dist\TikNick.exe`** לכל מקום — זה הכל. קובץ אחד.

בהרצה ראשונה, הנתונים נשמרים בתיקייה תקנית **נפרדת מה-EXE**:
- Windows: `%APPDATA%\TikNick\` (למשל `C:\Users\<שם>\AppData\Roaming\TikNick`)
- שם נוצרים `tiknick.db` (הנתונים) ו-`tiknick.log` (לוגים)

**יתרונות ההפרדה:**
- ✅ אפשר לשים את ה-EXE בכל מקום — גם `Program Files` — בלי בעיות הרשאות
- ✅ שדרוג גרסה: פשוט מחליפים את ה-EXE, הנתונים נשמרים
- ✅ גיבוי קל: מגבים רק את תיקיית `%APPDATA%\TikNick`
- ✅ מגרסה ישנה: אם היה DB ליד ה-EXE, הוא מועבר אוטומטית בהרצה הראשונה

## הערות
- הבנייה עצמה דורשת Python + pip פעם אחת. ה-EXE הסופי לא.
- זמן בנייה: 1-3 דקות.
- גודל ה-EXE: כ-15-30MB (כולל Python וכל התלויות).
- אייקון: נוסיף בהמשך (יש שורה מוכנה ב-TikNick.spec).

## הרצה מהמקור (למפתחים)
בלי לקמפל — `הפעל.bat` מריץ ישירות מ-Python.

## מה כלול בקימפול המלא (v0.1)
- ✅ **אייקון** — `icon.ico` (סיכה בצבע ענבר) מוטמע ב-EXE ובחלון
- ✅ **מאפייני קובץ** — שם, גרסה, תיאור (Properties → Details)
- ✅ **פורום "כללי"** — נוצר תמיד אוטומטית, מוגן ממחיקה
- ✅ **בלי חלון קונסולה** — משתמש רגיל לא רואה כלום
- ✅ **לוגים** — `tiknick.log` ליד ה-EXE
- ✅ **DB מקומי** — `tiknick.db` ליד ה-EXE, נשמר בין הפעלות
- ✅ **עצמאי לגמרי** — בלי Python, בלי pip, בלי רשת

## החלפת אייקון בעתיד
החלף את `icon.ico` בקובץ שלך (מומלץ multi-resolution: 16/32/48/256px)
ובנה מחדש. השם חייב להישאר `icon.ico`.
