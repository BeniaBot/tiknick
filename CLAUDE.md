# Tik-Nick — מסמך הקשרים (מקור אמת יחיד)

> נכתב מקריאה מלאה של כל הקוד; עודכן לגרסה **0.8.21** (2026-09).
> **`context_compress.md` ו-`project_map.md` מיושנים** (מתארים 0.2.5) — אל תסתמך עליהם.
> מדיניות גרסאות: להישאר ב-0.8.x — לבנימין יש עוד שיפורים מתוכננים לפני 0.9.

## מהות

תוכנת דסקטופ Windows בעברית (RTL) לניהול ומעקב ניקים בפורומים חרדיים.
PyWebView: backend פייתון + ממשק HTML/CSS/JS בחלון, SQLite מקומי, PyInstaller → EXE יחיד.
ריפו: `BeniaBot/tiknick` · רישיון שימוש אישי · המשתמשים הם דוברי עברית לא-טכניים.

## קבצים (heב-runtime)

| קובץ | שורות | תפקיד |
|---|---|---|
| `main.py` | ~890 | כניסה, DPI, נתיבים, לוגים, `class API` (כל method → `window.pywebview.api.*`), מנגנון עדכון עצמי, threads לסריקה/ניתוחים |
| `database.py` | ~1270 | כל שכבת SQLite: 11 טבלאות + FTS5, מנוע מקורות, ייבוא/ייצוא, CRUD |
| `scraper.py` | ~430 | סריקת **NodeBB + Discourse** (זיהוי פלטפורמה, עימוד, ריטריי, 429, cookie); `detect_platform`, `_scrape_nodebb`/`_scrape_discourse`, מיפוי לכל פלטפורמה, מיזוג עמוד-שלם דרך `db.merge_scraped_users`. פלטפורמות ללא API (XenForo/phpBB/custom) מרימות שגיאה ידידותית |
| `chazonishnik.py` | ~255 | ניתוח פעילות משתמש (פוסטים/לייקים/שעות) → דוח HTML; Chart.js מוטמע מ-`web/chart.umd.min.js` (עובד אופליין, fallback ל-CDN). עוגיית `express.sid` אופציונלית; כל פורום NodeBB דרך בורר בממשק |
| `stinknik.py` | ~197 | כל הפוסטים עם דיסלייקים → דוח HTML. לרוב בלי עוגייה |
| `web/index.html` | ~233 | שלד: sidebar, topbar, טבלה+כרטיסים, באנרים צפים, statusbar |
| `web/app.js` | ~3080 | כל לוגיקת הצד-לקוח (פירוט למטה) |
| `web/style.css` | ~1177 | ערכות dark/light + 8 מבטאים דרך `data-theme`/`data-accent`, צפיפות דרך `data-density` |

בנייה: `TikNick.spec` (onefile, windowed, אורז `web/`+`icon.ico`, hiddenimports ל-edgechromium) · `build.bat` · `version_info.txt` (0.8.0.0) · `הפעל.bat` להרצה מהמקור.

## נתיבים ולוגים

- `resource_path(rel)` — משאבים ארוזים (`sys._MEIPASS` ב-EXE).
- `data_dir()` — ב-EXE: `%APPDATA%\TikNick` (עם מיגרציה אוטומטית של DB ישן מליד ה-EXE); בפיתוח: ליד הסקריפט.
- לוגים: `tiknick.log` בתיקיית הנתונים, כולל excepthook גלובלי.

## מסד נתונים (11 טבלאות + FTS)

```
forums(name UNIQUE, color, url, profile_pattern, sort_order)     "כללי" מוגן ממחיקה
nicks(~26 עמודות)   ← זהו cache! הערך המוצג. מקור האמת ב-field_values
nick_contacts(nick_id, type, value, label, is_private)           CASCADE
nick_conflicts(nick_id, field_name, conflicting_value, ...)      CASCADE — כמעט vestigial, ר' למטה
nick_identities(a, b, UNIQUE)                                    קבוצות טרנזיטיביות מלאות (כל זוג נשמר)
settings(key, value)          display_* / forumio_* / conflict_policy / my_trust / import_manual_conflicts
sync_settings(field_key, synced)                                 אילו שדות מיוצאים בקובץ
import_sources(...)           לוג ייבואים ישן (לתאימות; גם import_log.txt ליד ה-DB)
shelved_values(...)           ערכים "על המדף" + promote_shelved (החלפה הפיכה)
sources(kind: me|scrape|import, trust 1-10, absolute)            id=1 = "אני" (trust 10), scrape=9
field_values(nick_id, field_name, value, source_id, UNIQUE שלישייה)  כל ערך שהגיע אי-פעם
nicks_fts(FTS5, unicode61 remove_diacritics 2) + 3 טריגרים       חיפוש; fallback ל-LIKE אם אין FTS5
```

WAL + foreign_keys ON. אינדקסים על username/forum/updated_at/trust_level/fk-ים.
מיגרציות: `_migrate()` מוסיף עמודות חסרות; `_backfill_sources()` חד-פעמי (דגל `backfill_sources_done`);
`_init_fts()` בונה מחדש אוטומטית סכימת FTS ישנה (מ-0.8.1 החיפוש כולל גם full_name ו-address —
`_SEARCH_COLS` הוא מקור האמת לעמודות החיפוש, גם ל-FTS וגם לנפילת ה-LIKE).

## מנוע המקורות — הלוגיקה המרכזית

- כל כתיבת ערך: `record_field_value(nick, field, value, source_id)` → upsert ל-`field_values` → הכרעה → עדכון ה-cache ב-`nicks`.
- **פנימיות (מ-0.8.1)**: `_upsert_field_value(conn,...)` + `_winner_for(field, rows)` + `_resolve_fields_conn(conn, nick, fields)` — כולן עובדות על חיבור קיים; ההכרעה המרובה מעדכנת כמה שדות ב-UPDATE אחד (חוסך גם שכתובי FTS, כי הטריגרים משכתבים את כל שורת ה-FTS בכל UPDATE). כתיבה חדשה בתפזורת? לעבוד דרכן על חיבור אחד, לא לפתוח חיבור פר-שדה.
- ניצחון: `absolute=1` → אינסוף; אחרת trust; שובר שוויון — `created_at` חדש יותר.
- **כללים מיוחדים ב-`resolve_field`:**
  - `reputation` — רק ערכי סריקה נחשבים, החדש ביותר מנצח.
  - `status` — מקור סריקה מקבל אמינות אבסולוטית (הרחקה בפורום גוברת על עריכה ידנית — **מכוון**; המשתמש לא יכול לדרוס סטטוס סרוק דרך הדיאלוג).
- `_NON_SOURCED` (לא במנוע): forum, username, source, trust_level, scraped_real_name, scraped_email, created_at, updated_at.
- ריקון שדה ידני = מחיקת התרומה של "אני" + הכרעה מחדש (הערך הבא באמינות עולה).
- עקיפות מפורשות: `force_field_value` (כתיבה ישירה ל-cache) — משמשת ב"סנכרן נבחרים" וב-`apply_import_conflict`.
- `update_source`/`delete_source` מריצים resolve מחדש לכל השדות המושפעים. absolute מותר רק ל"אני".

## סריקה (סנכרון לאינטרנט)

- **המסלול המהיר (מ-0.8.1)**: `db.merge_scraped_users(forum, pairs, label)` — עמוד שלם בחיבור וטרנזקציה אחת, עם זיהוי שינויים מול הערכים הקודמים של מקור הסריקה (סריקה חוזרת ללא שינוי כמעט לא כותבת; נמדד פי ~400 מהמסלול שדה-שדה). scraper.handle_users ממפה עמוד ואז קורא לזה.
- `_SCRAPE_MERGE_FIELDS`: groups, reputation, full_name, email, address, status, join_date, post_count, avatar_url, nick_color, avatar_image, extra_info, forum_uid. **בכוונה לא**: phone, real_name, notes, private_notes (פרטיות המשתמש).
- ניק חדש מסריקה: trust_level=4, source="NodeBB:<פורום>"; scraped_email מתעדכן גם במיזוג. מיובא מקובץ: trust_level=3.
- שלושה מצבים ב-main.py: `start_scrape` (פורום אחד), `start_scrape_all` (הכול ברצף, דילוג על כשל + skip ידני, התקדמות חיה מצטברת), `sync_selected_online` (נבחרים; הסרוק תמיד מנצח דרך `db.force_scraped_values` — חיבור אחד לניק).
- שגיאות הרשאה: `scraper.AuthRequired` (תת-מחלקה של ScrapeError) על 401/403 — `check_forum` מזהה דרכה דרישת התחברות ומציע עוגייה.
- מצב משותף ב-module globals: `_scrape_state` + `threading.Event` לביטול/דילוג. ה-UI סוקר ב-polling (`get_scrape_progress`, כל 700ms).
- KNOWN_FORUMS ב-database.py: 16 פורומים עם דגלים `needs_login` (המטבח) ו-`scrapable:False` (לתורה — לא NodeBB).
- נימוס: UA מזוהה, 0.6s בין עמודים, כיבוד Retry-After, עמוד שנכשל מדולג.

## ייבוא/ייצוא

- פורמט `.tiknick` (JSON): `{version:3, exported_at, exported_fields, export_mode, nicks[], identity_groups[], counts}`. רק שדות מסונכרנים (`sync_settings`) ורק פורומים שלא הוחרגו (`forumio_*`). `nicks[i].contacts` = אנשי קשר גלויים; `identity_groups` = רשימות של `{forum, username}`. `exported_fields` הוא שמות עמודות בלבד — זו התאימות לאחור. גרסה 2 עדיין נטענת.
- **ייבוא CSV/TSV**: `csv_import.py` (טהור, בלי DB) → אותו `db.import_data`. ראו סעיף 0.8.7.
- **תצוגה מקדימה**: `preview_import` — כל ייבוא עובר דרכה לפני כתיבה.
- זרימת ייבוא דו-שלבית: `load_import_file` (בדיקת פורומים זרים, נשמר ב-`self._pending_import`) → דיאלוג פרטי מקור (שם/הערות/trust) → מיפוי פורומים → `confirm_import`.
- התנגשות = ערך שונה בשדה מלא: אוטומטי לפי אמינות, או ידני אחד-אחד (`import_manual_conflicts=1`, עם "החל על הכול").

## עדכון עצמי

- `check_for_updates`: GitHub `releases/latest`, השוואת 3 מספרים, איתור נכס `.exe`.
- **כלל זהב: `APP_VERSION` (main.py:59) == tag בלי v** (0.8.0 == v0.8.0), אחרת הבדיקה נשברת.
- `download_update` → `TikNick_new.exe` ליד ה-EXE → `apply_update` כותב batch (ממתין לסגירה, מנקה משתני `_MEI*`/`_PYI*`, עד 15 ניסיונות move, מפעיל מחדש) → `os._exit(0)`.
- שחרור גרסה: עדכן APP_VERSION + version_info.txt → commit+push → `build.bat` → Release עם tag תואם + `dist\TikNick.exe` מצורף.

## app.js — מפה מהירה

- State גלובלי `S` (nicks, forums, multiSelected:Set, sortCol, loadToken נגד race), `COLS` (16 עמודות עם renderers), `DISPLAY`.
- גשר: `api(method, ...args)` גנרי + `waitForApi` (עד 10s).
- **טבלה = virtual scrolling** (רק השורות הנראות + spacers, `rowHeight` נמדד); **כרטיסים = טעינה הדרגתית** במנות של 120 בגלילה.
- דיאלוגים דרך `openModal(title, html, buttons, size)` — HTML נבנה כמחרוזות עם inline handlers; פונקציות מחוברות ל-`window.*` כשנדרש. `esc()` לכל ערך משתמש.
- תיוג `@ניק` בשדות `.tag-field`: רווחים→קו תחתון, השלמה אוטומטית, ריחוף, קליק פותח את הניק.
- מיון: `has_info` תמיד ראשון (גם במיון ידני). SQL ממיין `has_info DESC, trust_level DESC, updated_at DESC`.
- ניתוחים (סריקה/Chazonishnik/Stinknik) רצים ברקע עם באנר צף; חידוש polling אוטומטי אם רצים בעת טעינת הדף.
- דוחות מוצגים ב-iframe דרך `srcdoc`; שמירה דרך דיאלוג קובץ של pywebview.

## תשתית UI/עיצוב

- ערכות: dark (ברירת מחדל, פחם חם) / light (קרם) / system; 8 מבטאים (ברירת מחדל amber); צפיפות compact/normal/cozy — הכול CSS variables על `data-*` attributes, נשמר ב-settings.
- `user-select:none` גלובלי, film-grain overlay, אנימציות spring.

## מה תוקן ונוסף ב-0.8.21 (2026-09-06)

### 🚨 השורש: NodeBB מחזיר טקסט **מקודד ל-HTML**
בנימין צילם ניק שמוצג כ-`ע&quot;ה דכו&quot;ע`, והוסיף: "הוא גם גורם שלחיצה
פתח בדפדפן לא מוצאת". שתי התופעות הן אותו באג אחד.

זו **לא** בריחה כפולה בתצוגה — כל מסלולי התצוגה נבדקו והם תקינים. הערך
עצמו **נשמר במאגר מקודד**: ה-API של NodeBB מחזיר `username` בצורת התצוגה
שלו, כלומר `ע&quot;ה`, והסורק שמר אותו כפי שהוא מאז ומעולם. משם:
- הטבלה מציגה את הישות כטקסט (נכון — זה מה ששמור).
- **`buildProfileUrl` בונה את הקישור מהשם המקודד**, ולכן הפורום לא מוצא
  את המשתמש. זה החצי שכואב.

**התיקון בגבול הרשת**: `scraper._txt()` (ומקבילותיה בדוחות) מריצות
`html.unescape` על כל טקסט שמגיע מהפורום — שם משתמש, שם מלא, קבוצות,
מיקום, "אודות", חתימה, וכותרות נושאים בדוחות. משם והלאה כל התוכנה עובדת
על טקסט אמיתי.

**ולעבר**: `db.count_encoded_values()` / `fix_encoded_values()` וכפתור
"🔤 תקן שמות מקודדים" ב-"🩺 בריאות המאגר". מפענח במקום גם ב-`nicks` וגם
ב-`field_values` (`UPDATE OR REPLACE` — הפענוח יכול להתנגש בשורה קיימת של
אותו מקור), עם גיבוי אוטומטי לפני. **הערות שהמשתמש כתב בעצמו אינן נוגעות**:
`_ENCODED_FIELDS` כולל רק שדות שמקורם בפורום, כי מי שהקליד `&quot;` ידנית
התכוון לכתוב בדיוק את זה.

### 🚨 מייל נקרא כתיוג
"כל פעם שכותבים מייל של מישהו התוכנה חושבת שמנסים לתייג ניק בשם גימייל
נקודה קום." הכלל היה `@` **בכל מקום**, ולכן `beni@gmail.com` ייצר `@gmail.com`.
זה קרה בשני מקומות שלא ידעו זה על זה — ההשלמה בזמן ההקלדה **וגם** התצוגה
של הטקסט השמור, שצייר את הדומיין כתג לחיץ.

עכשיו יש כלל אחד לשניהם: **תיוג פותח מילה** (תחילת טקסט או אחרי רווח/פיסוק),
ומה שנראה כמו דומיין (`TAG_DOMAIN_RX`) לעולם אינו ניק. בנוסף, פיסוק סוגר
בסוף משפט כבר לא נבלע לתוך השם (`(@דוד)` הוא תיוג של דוד ועוד סוגר) —
אבל **גרשיים כן נשארות**, כי הן חלק לגיטימי משם עברי.

`tests/test_tags.js` שולף את שתי הפונקציות מ-`app.js` ומריץ אותן ב-vm.
שים לב: `const` בתוך `vm` **אינו** נחשף על אובייקט ההקשר (רק `var` ופונקציות) —
לכן הבדיקה מוסיפה `this.TAG_OPEN = TAG_OPEN` במפורש.

### 🛡️ רשת ופרוקסי — `net.py`
בהשראת PR #2, כתוב מחדש ומותאם. **כל** בקשה יוצאת (סריקה, Chazonishnik,
Stinknik, בדיקת עדכון והורדתו) עוברת דרך `net.urlopen`. שלושה מצבים:
`system` (ברירת מחדל — בדיוק ההתנהגות שהייתה), `off` (ישיר), `manual`.

הבדלים מהותיים מה-PR:
- **בלי כפתור חדש בתפריט.** בנימין ביקש פעמיים תפריט קצר יותר, ולכן החלון
  נפתח מתוך "🌐 סנכרון לאינטרנט" — הפעולה היחידה שבגללה בכלל יוצאים החוצה.
- **הבדיקה מכוונת לפורום שהמשתמש עצמו הגדיר** (בורר), ולא לכתובת קשיחה.
- **401/403 נחשבים הצלחה** של החיבור: הפורום ענה, הוא רק דורש התחברות.
- `apply` בונה את ה-opener *לפני* שהוא מחליף את המצב, כך שהגדרה פסולה לא
  משאירה את התוכנה בלי רשת. הגדרה שמורה פגומה נופלת ל-`system` עם רישום.

**מלכודת שנמדדה**: `ProxyHandler({})` לא נכנס בכלל לרשימת ה-handlers של
ה-opener — ובמקביל הוא *מוציא* את ProxyHandler ברירת המחדל. התוצאה היא
בדיוק "ישיר", אבל בדיקה תמימה על `.proxies` נראית ככישלון.

### 🌐 סימון כמה פורומים לסריקה
בהשראת PR #3. בורר הפורום היחיד הוחלף ברשימת תיבות סימון, וליד כל פורום
מתי נסרק לאחרונה. פורום אחד → המסלול הקיים `start_scrape` (עם העוגייה
שבשדה); כמה → `start_scrape_all` עם `only_forums`, ואז לכל פורום משמשת רק
**העוגייה השמורה שלו** — כלל הפרטיות מ-0.8.3 לא נחלש.

הפורום ה"פעיל" (`syncActive`) הוא הראשון שסומן: שדה העוגייה, הרמז
ו"בדוק פורום" הם מטבעם חד-פורומיים ולכן מתייחסים אליו. `S.lastScrapes`
נטען עכשיו **לפני** בניית הרשימה, אחרת השורות היו נבנות בלי הזמנים.

**בדיקות**: `tests/test_net.py` (44) ו-`tests/test_tags.js` (18). 19 חבילות.

## מה נוסף ב-0.8.20 (2026-09-06) — הדוחות באנגלית, והתפריט מתקצר

### הדוחות והגיליון להדפסה
`web/i18n.js` מתרגם את ה-DOM של החלון הראשי, אבל הדוחות של Chazonishnik
ו-Stinknik יושבים ב-iframe עם `sandbox` **ללא** `allow-same-origin` — ולכן
ה-observer לא יכול לגעת בהם — והגיליון להדפסה בכלל נפתח בדפדפן החיצוני.
לכן נוסף `i18n.py` בצד הפייתון.

**הדפוס שמאפשר את זה בבטחה**: כל הדוחות בנויים כתבנית סטטית עם מצייני מקום
(`__USER__`, `__FIELDS__`) שמוחלפים בנתונים **אחרי** הבנייה. `translate_template`
רץ על **התבנית**, לפני ההזרקה — כלומר הוא רואה אך ורק טקסט ממשק, ולעולם לא
שם משתמש או כותרת נושא מהפורום.

**שתי מלכודות שנתפסו במדידה**:
1. **מפתח קצר בן מילה אחת הורס טקסט חופשי.** בקטלוג יש `"כל" → "Every"` ו-
   `"שני" → "Monday"`, וההחלפה הפכה `// כל טקסט` ל-`// Every טקסט` ו-
   `"משתמש שני"` ל-`"משתמש Monday"`. מנוע התבניות מקבל עכשיו רק מפתחות
   באורך ≥6 או רב-מיליים. מילים קצרות שכן נחוצות יושבות במפה **מקומית** לכל
   מודול (`_LOCAL_EN` / `_TPL_EN` / `_CMP_EN`) ולא בקטלוג המשותף.
2. **הערות קוד עבריות בתוך `<script>` תורגמו.** הן לא ממשק. כל התאמה שקודמת
   לה `//` באותה שורה מדולגת.

בנוסף `dir="rtl"`/`lang="he"` הופכים ל-`ltr`/`en`, ותוויות שנבנות בקוד ולא
בתבנית (שמות הימים בגרפים, כותרות המקטעים בגיליון, "למעבר לפוסט") עוברות
`t()` מפורש.

**`i18n_en.json` נוסף ל-`TikNick.spec`.** בלעדיו הקטלוג לא נארז ב-EXE והדוחות
היו נשארים בעברית **רק בגרסה הבנויה** — באג שלא היה מתגלה בהרצה מהמקור.

### התפריט: 19 → 16
בנימין: "גיבוי מלא · שחזור מגיבוי · סל מחזור לכאורה מיותרים בסרגל הימני.
התכוונתי שתחזיר רק את מה שממש ממש ותיק."

שלושתם ירדו. הם פעולות על **הקובץ**, וכולם כבר יושבים ב-"🩺 בריאות המאגר"
לצד מצב המאגר עצמו. עם 16 כפתורים חזר אוויר לשורת התפריט (`padding:5px→7px`,
`icon:15px→16px`, `nav-group:5px→7px`) — שורה של 35px, וסה"כ 716px: עדיין
נכנס בדיוק בלי גלילה.

**בדיקות**: `tests/test_i18n.py` גדל ל-45 — כולל שהתבניות יוצאות נקיות
לגמרי, שהערת קוד לא מתורגמת, שמפתח קצר לא נכנס למנוע, ושבעברית הכול חוזר
בית-בית.

## מה נוסף ב-0.8.19 (2026-09-06) — ממשק באנגלית

בורר "שפה / Language" בהגדרות התצוגה. באנגלית כל הממשק מתורגם והפריסה מתהפכת
ל-LTR (הסרגל עובר שמאלה, הטבלה מתהפכת). **ברירת המחדל עברית ולא השתנה בה כלום.**

### למה תרגום ב-DOM ולא `t()` בכל אתר קריאה
הממשק בנוי כמחרוזות HTML שמורכבות בזמן ריצה בכ-1200 מקומות ב-`app.js`. מעבר
ל-`t('...')` בכל אחד מהם הוא שכתוב של כל הקובץ, עם סיכון רגרסיה בכל שורה.
במקום זה `web/i18n.js` מתרגם את ה-DOM **אחרי** שהוא נבנה:

- `MutationObserver` על `document.body` מתרגם כל צומת שנוסף — חלונות, טוסטים,
  שורות טבלה, הכול. **הוא מותקן רק כשהשפה אנגלית**; בעברית הוא לא קיים ואף
  שורת קוד מהמנוע לא רצה.
- שלוש שכבות התאמה: התאמה מדויקת, התאמה אחרי נרמול רווחים (פסקה ב-HTML מגיעה
  עם שבירות שורה והזחה), ותבניות עם `{1}`/`{2}` לטקסט שיש בו ערכים מוטמעים.
- **`i18nBlocks`** — פסקה שיש בתוכה `<b>` מפוצלת לכמה צמתי טקסט, וכל אחד לבדו
  אינו מפתח בקטלוג. לכן נעשית גם השוואה ברמת האלמנט — אבל **רק** כשאין בתוכו
  `[onclick]`, `[id]`, `a`, `button` או שדה קלט, כדי לא למחוק התנהגות.
  מה שכן מכיל אלמנט אינטראקטיבי מקבל את הפרגמנטים כרשומות נפרדות.

### מה לא מתורגם, במכוון
`#tbody` ו-`#cards-grid` מסומנים `data-no-i18n`. שמות ניקים, שמות פורומים
וכל מה שנסרק הם **נתונים**, וניק ששמו במקרה "ייצוא" לא אמור להפוך ל-"Export".
גם `INPUT`/`TEXTAREA`/`CODE`/`IFRAME` מוחרגים.

### הקטלוג
`i18n_catalog.json` (1,083 מחרוזות) נוצר בחילוץ מקבילי — סוכן לכל מקטע קוד,
ואחריו סוכן שני שמחפש מה הוחמץ ומתקן ניסוח. `i18n_extra.json` הוא ההשלמות
הידניות שנכתבו אחרי החילוץ. `tools/build_i18n.py` מרכיב מהם את `web/i18n.js`,
ו-`tests/test_i18n.py` נכשל אם הקובץ שנוצר אינו מסונכרן עם הקטלוג.

### הפריסה
בלוק `[data-lang="en"]` ב-`style.css` הופך את הכללים ה**פיזיים** שלא מתהפכים
לבד: `direction`, צד הגבול של הסרגל, יישור הכותרות, `margin-right:auto`,
ידית שינוי הרוחב, וכיווני ה-`translateX`. גם גרירת רוחב העמודה מודעת עכשיו
לכיוון — `dx` הוא `startX - clientX` ב-RTL ו-`clientX - startX` ב-LTR.

**מה נשאר בעברית**: הדוחות של Chazonishnik/Stinknik והגיליון להדפסה. הם
מסמכים נפרדים שנוצרים בפייתון, ולכן ה-observer לא מגיע אליהם.

**בדיקות**: `tests/test_i18n.py` (30). אומת בדפדפן על 15 חלונות: אחרי המעבר
לא נשאר בהם טקסט עברי מלבד שמות פורומים ו"עברית" בבורר עצמו.

## מה תוקן ב-0.8.18 (2026-09-05) — החזרת הוותיקים, והשאר מהביקורת

### 🚨 רגרסיה: הקיצוץ ב-0.8.17 לקח גם ותיקים
בנימין: "נעלמו לשוניות מהתפריט הימני גם וותיקות! אלו שהיו עוד ב-v0.8! כמו
אודות ואולי עוד. הן חשובות יותר."

צדק. הבקשה המקורית הייתה על **מה שנוסף בעדכונים האחרונים**, ואני הורדתי גם
כפתורים שקיימים מ-0.8.0-0.8.4. הוחזרו: **אודות, גיבוי מלא, שחזור מגיבוי,
סל מחזור, איפוס נתונים** — וגם קבוצת "מתקדם" חזרה.

**הכלל שנגזר מזה**: מה שהיה שם לפני גל התוספות הוא מה שהמשתמש מכיר ומצפה לו.
קיצוץ תפריט נוגע רק במה שנוסף לאחרונה, לעולם לא בשריר הוותיק.

מה שנשאר מקונן הוא בדיוק תוספות 0.8.5-0.8.10: הצעות זהות (לשונית ב"זהויות"),
סריקה מתוזמנת ויומן סריקות (בתוך הסנכרון), נצפו לאחרונה (המצב הריק של תצוגת
משתמש), סטטיסטיקות (לחיצה על המונים בסרגל העליון).

**19 כפתורים ב-5 קבוצות = 716px בדיוק** — נכנס במסך בלי גלילה. זה הושג ע"י
הידוק מטריקות התפריט (`padding:8px→5px`, `icon:17px→15px`,
`nav-group margin:9px→5px`), כלומר שורת תפריט של 30px במקום 39px. הצפיפות
(`data-density`) משפיעה עכשיו גם על התפריט — קודם היא הצהירה שהיא מכווצת ולא
נגעה בו בכלל.

### שאר הממצאים מהביקורת של 0.8.17
- **"🍪 איך משיגים?" השמיד את טופס הסנכרון** — הוא קרא ל-`openChazonishnik()`,
  ו-`openModal` סוגר את החלון הנוכחי. ההסבר חולץ ל-`cookieHelpHtml()` ונפתח
  עכשיו **בתוך** החלון (`toggleCookieHelp`). אותו תיקון ל-Stinknik.
- **ה-✕ התעלם מ-`dismissable:false`** — טופס עריכה מלא נמחק בלחיצה אחת בזמן
  ש-Esc ולחיצת רקע דווקא היו חסומים, ובחלון "מייבא…" הוא נראה כמו ביטול
  בזמן שהייבוא ממשיך לכתוב. עכשיו `requestCloseModal()` שואל.
- **ניגודיות בערכה הבהירה**: `--accent-2` שימש כצבע *טקסט* לכל כותרות
  המקטעים (2.2:1). נוסף טוקן `--accent-text` (בהיר=`--accent-dk`,
  כהה=`--accent-2`). `--warn` הוכהה ל-`#8a5a00`, `--subtext` ל-`#7b6c60`.
- **שם הפורום נצבע בצבע הפורום על אותו צבע ב-13%** — הטקסט החלש ביותר בכל
  שורה בטבלה. הצבע נשאר כרקע וכטבעת, הטקסט עבר ל-`var(--text)`.
- **אות האווטאר הייתה `#fff` קשיח** ונעלמה על ליים/ענבר. נוספה `fgOn(bg)`
  שבוחרת שחור/לבן לפי בהירות הרקע בפועל.
- **`tbody td:has(.uname-dot){display:flex;max-width:230px}`** הוציא את התא
  מהטבלה (קו ההפרדה נשבר) וחסם את השם ב-230px גם בעמודה רחבה. הוסר.
- **`.btn-primary{overflow:hidden}`** איפס את ה-`min-width` שלו, ולכן דווקא
  הכפתור הראשי היה היחיד שנמעך. הוחלף ב-`clip-path`.
- **הכרטיסים**: הגלילה הווירטואלית מניחה גובה שורה אחיד ומודדת רק את הראשונה.
  `min-height` הוחלף בגובה קבוע.
- **מסגרות הדוחות** היו `height:68vh` בתוך חלון `max-height:90vh` — שני
  גוללים ותחתית חתוכה. `fillModalBody()` הופך את גוף החלון לעמודת flex.
- **חץ "ישן → חדש"** התהפך לפי תוכן השורה. עבר ל-`<bdi>` + `←`.
- **`(MB 12.4)`** — היחידה לפני המספר. `mb()` עוטף ב-LRI/PDI.
- **רשימת ההשלמה של @** נפתחה בקצה השמאלי של השדה, הצד ההפוך מהסמן ב-RTL.
- **`#bg-tasks`** חתך את שורת המצב (`bottom:16px` → `47px`).
- **"סגור" היה `btn-primary`** ב-8 חלונות ו-`btn-ghost` באחרים; ו-`btn-warning`
  היה אותו גרדיאנט כמו `btn-primary`, כך ש"🌍 סרוק הכל" נראה זהה
  ל"התחל סריקה". שניהם יושרו.
- **אשף הייבוא** קפץ בין 860/480/700 פיקסל, וכפתור "📥 ייבא" פתח עוד חלון
  במקום לייבא. גודל אחיד + "המשך לתצוגה מקדימה".
- גם: זוהר הכפתורים בתפריט דהה בצד הריק, נקודות הפורום קיבלו הילה לבנה
  (`currentColor` היה צבע הטקסט), הצעות זהות נראו כמו אזהרות, "▶ הרץ עכשיו"
  החזיר טוסט אדום על מצב תקין, ו"שמור" בהגדרות סנכרון לא סגר את החלון.

## מה תוקן ב-0.8.17 (2026-09-05) — התפריט והתצוגה

**דיווח**: "נוספו הרבה לשוניות — לכל אחת באמת חייבת להיות? ובכל מיני מקומות יש
שיבושי תצוגה קשים, למשל בתוך חלון בריאות המאגר".

**מקור**: ביקורת של 8 עדשות במקביל (פריסת פוטר, RTL, גלישה, ניגודיות בערכות,
ארגון התפריט, התאמה לחלון קטן, עקביות בין חלונות) עם שני מפריכים לכל קבוצת
ממצאים — 76 ממצאים ששרדו. כל מה שתוקן כאן נמדד בפועל בארגז החול לפני ואחרי.

### 🚨 ארבעה כפתורים שצוירו מחוץ לחלון
`.modal-footer` הוא flex **בלי `flex-wrap`**, ו-`.btn` הוא `white-space:nowrap`
— כלומר `min-width` של כל כפתור הוא רוחב הטקסט המלא ו-`flex-shrink` לא יכול
לכווץ אותו. `openDbHealth` העביר **7 כפתורים** לחלון `modal-sm` ברוחב 480px:
נמדד **915px של כפתורים ברוחב פנוי של 426px**, והתוצאה — ארבעה כפתורים
(*יומן ייבואים*, *תקן קבוצות זהות*, *גבה עכשיו*, *פתח תיקיית נתונים*) צוירו
מחוץ לגבול החלון ו-`.modal{overflow:hidden}` חתך אותם, ו"סגור" נמעך מ-60px
ל-36px. `scrollWidth == clientWidth`, כלומר **גם לא הייתה דרך לגלול אליהם** —
לא בעכבר, לא במקלדת. שלוש מהפעולות האלה לא היו זמינות בשום מקום אחר בתוכנה.

**תוקן בשלוש שכבות**: `flex-wrap:wrap` ל-`.modal-footer` (רשת ביטחון לכל חלון
עתידי), `openModal` לא מצייר פוטר כשאין כפתורים (שני חלונות ההמתנה של הייבוא
הציגו פס ריק בגובה 33px עם קו מפריד), והפעולות עצמן ירדו לגוף החלון בשורות
שנשברות — ב-footer נשאר "סגור" בלבד.

### התפריט: 24 → 14
נמדד: 24 כפתורים = **1070px של תפריט בעמודה של 716px**, כלומר שליש מהתפריט
מתחת לקו הקיפול (54% בחלון המינימלי 1000×600) — מאחורי פס גלילה של 4px בצבע
`--border`, יחס ניגודיות 1.45:1 מול רקע הסרגל. כלומר "סנכרון לאינטרנט", הפיצ'ר
המרכזי, פשוט לא היה על המסך.

**עכשיו 14 כפתורים ב-4 קבוצות, סה"כ 716px — נכנס בדיוק, בלי גלילה.** כל מה
שהוסר קיבל בית בחלון שכבר מחזיק את הנושא:

| ירד מהתפריט | הבית החדש |
|---|---|
| 🔗 הצעות זהות | לשונית שנייה בחלון **🗺️ זהויות** |
| ⏰ סריקה מתוזמנת · 🕒 יומן סריקות | גוף חלון **🌐 סנכרון לאינטרנט** |
| 🕘 נצפו לאחרונה | המצב הריק של **🔎 תצוגת משתמש** |
| 📊 סטטיסטיקות | לחיצה על מוני הסרגל העליון |
| 🗄️ גיבוי מלא · ♻️ שחזור · 🗑️ סל מחזור · 🔴 איפוס | חלון **🩺 המאגר** |
| אודות | שורת הגרסה בתחתית הסרגל (שכבר פתחה אותו — היו שתי נקודות עדכון פועמות זו מעל זו) |

`openModal` סוגר את החלון הנוכחי ואין לו מחסנית, ולכן כל חלון שנפתח מתוך חלון
אחר מקבל עכשיו כפתור **"↩ חזרה"**.

### ניגודיות ו-RTL (נמדד, לא הוערך)
- **`color-scheme` לא הוצהר כלל** — כל תיבות הסימון והרדיו של המערכת צוירו
  בהיר על ערכה כהה. מוצהר עכשיו לכל ערכה.
- **`.nav-btn.danger{color:#f87171}`** — אדום קשיח שנבחר לערכה כהה: **2.21:1**
  על הסרגל הבהיר. עבר ל-`var(--danger)`, ו-`--danger` הבהיר הוכהה ל-`#b3261e`:
  **5.23:1** (וגם טקסט לבן על `.btn-danger` עלה מ-3.9 ל-7.0).
- **`.nav-label`** ב-`--subtext`: 2.33:1 → `--text-dim`: **5.07:1**.
- **`.form-input{direction:rtl}`** גבר על **כל** `dir="ltr"` בתוכנה (כלל מחבר
  גובר על כלל דפדפן), ולכן שדה העוגייה, כתובות URL ומוניטין שלילי הוצגו הפוך.
  הכלל צומצם ל-`:not([dir])`.
- **`#bg-tasks` היה על `right:16px`** — בפריסת RTL זה בדיוק הצד של הסרגל, והבאנרים
  ישבו על תחתית התפריט. עברו לצד השני.
- **`.modal-title` הוא `display:flex`**, ולכן ה-`::after` שאמור להיות קו תחתון
  הפך לפריט flex וצויר כמקף *לצד* הכותרת. הוצא מזרימת הפלקס.
- **הסרגל העליון**: ל-`.theme-toggle-btn` יש `overflow:hidden`, ולכן ה-min-width
  שלו הוא 0 והוא התכווץ לפס בחלון צר. הפקדים הקטנים קיבלו `flex-shrink:0`.

**לקח**: `min-width` של פריט flex הוא `min-content` — אלא אם יש `overflow:hidden`,
ואז הוא 0. שני הכיוונים מייצרים באג: בלי overflow הכפתורים גולשים מחוץ לחלון,
ואיתו הם נמחצים לפס. `flex-wrap` הוא התשובה לשניהם.

## מה השתנה ב-0.8.16 (2026-09-04) — סדר התפריט

Chazonishnik הועבר בקבוצת "כלים" למקום הרביעי, מיד לפני Stinknik — שני
כלי הניתוח יושבים עכשיו זה ליד זה, אחרי הסנכרון והדוחות. שינוי סדר בלבד
ב-`web/index.html`; אין שינוי קוד. שוחררה גם כדי לאמת שהעדכון העצמי עובד
מקצה לקצה מ-0.8.15 אחרי תיקון המתקין.

## מה תוקן ב-0.8.15 (2026-09-04) — התיקון עבר למתקין

**דיווח**: עדכון מ-0.8.12 ל-0.8.14 "נתקע כרגיל".

### התובנה המרכזית
**בעדכון מגרסה ישנה, הקוד היחיד שהוא חדש הוא ה-Setup.** כל תיקון שיושב
באפליקציה עוזר רק למי שכבר עדכן — כלומר לא למי שתקוע. גרוע מזה: ה-`AppMutex`
שהוספתי ב-0.8.13 **הפך כל עדכון מלקוח ישן לבלתי אפשרי**. לקוח ישן מריץ את
ה-Setup במקביל לתוכנה החיה → המנעול תפוס → ובהתקנה שקטה ההודעה מדוכאת ו-Inno
עונה עליה `Cancel` בעצמו (`Got EAbort exception`). זו הייתה רגרסיה שלי.

### מבנה שני התהליכים
נצפה על המכונה החיה: **שני** תהליכי `TikNick.exe` שנוצרו בהפרש שנייה — אחד עם
חלון (`MainWindowHandle=5312072`) ואחד **בלי** (`0`). זה מבנה PyInstaller
onefile: תהליך-אב שמחלץ ל-`_MEI` ומפעיל תהליך-בן. סגירת החלון הורגת את הבן;
**האב נשאר** ומחזיק את דמות ה-EXE, ולכן `DeleteFile` נכשל ב-Access denied.
Restart Manager לא הצליח לסגור אותו ("Some applications could not be shut down").

### התיקון
- **`AppMutex` הוסר.**
- **`[Code]` עם `PrepareToInstall`** שסוגר בפועל כל תהליך `TikNick` דרך
  PowerShell `Stop-Process -Force`, ממש לפני העתקת הקבצים. אין מה לאבד:
  לתהליך שנשאר אין חלון ואין מצב לא שמור (SQLite עם WAL כותב בכל פעולה).
- זה עובד **לכל גרסת לקוח**, גם כזו שלא תעודכן לעולם — כי הקוד יושב במתקין.

**אומת מקצה לקצה** מול ההתקנה החיה: לפני — 0.8.12 עם שני תהליכים כולל היתום;
אחרי — `exit code 0`, `Installation process succeeded`, גרסה **0.8.15**,
התוכנה עלתה, והמאגר (99MB) שלם.

**לקח**: כשמנגנון עדכון שבור, התיקון חייב לשבת בצד שמתחלף — המתקין — ולא בצד
שצריך את התיקון כדי להתקין את התיקון.

## מה תוקן ב-0.8.14 (2026-09-04) — תהליך יתום, ו-tasklist ששיקר

**דיווח**: "התוכנה סגורה ולא מופיעה במנהל המשימות" — וההתקנה עדיין נכשלה.
ואז: עדכון מ-0.8.12 ל-0.8.13 נכשל שוב.

### 1. תהליך בלי חלון
על המכונה החיה נמצא `TikNick.exe` **בלי חלון בכלל** (`MainWindowHandle = 0`,
שני threads ב-Wait, 0.8 שניות CPU) ששרד מניסיון עדכון קודם. הוא לא מופיע
בלשונית "אפליקציות" של מנהל המשימות כי אין לו חלון, אבל כל עוד הוא חי דמות
ה-EXE ממופה לזיכרון ו-`DeleteFile` נכשל ב-`Access denied`. אומת בשני הכיוונים:
**שינוי שם הצליח בזמן שהמחיקה נכשלה** (ההתנהגות המדויקת של Windows לקובץ הרצה),
ואחרי סגירת התהליך המחיקה עברה.

### 2. `AppMutex` + `/SUPPRESSMSGBOXES` = ביטול אוטומטי
ה-`AppMutex` שנוסף ב-0.8.13 **עבד** — יומן ההתקנה מראה שהוא זיהה את התוכנה
והציג את ההודעה החדשה. אבל בעדכון שקט ההודעה מדוכאת, ו-Inno עונה עליה
**Cancel** אוטומטית:
```
Defaulting to Cancel for suppressed message box (OK/Cancel):
  Tik-Nick פתוחה כרגע...
Got EAbort exception.
```
כלומר התיקון של 0.8.13 הפך בעצמו לחוסם, כל עוד משהו מחזיק את המנעול.

### 3. השורש האמיתי: `tasklist /FI` החזיר "לא נמצא" על תהליך חי
ההמתנה בעדכון נשענה מאז 0.8.3 על
`tasklist /FI "IMAGENAME eq X" | find /I "X"`. נמדד כאן, מתוך קובץ batch אמיתי:
**התהליך רץ, `Get-Process` מוצא אותו, ו-`tasklist /FI` מחזיר ריק.** כלומר לולאת
ההמתנה יכלה להסתיים מיד — וההתקנה לרוץ אל תוך תוכנה פתוחה. זה מסביר את כל
סדרת הכישלונות, ולא רק את האחרון.

**תוקן**: שני מסלולי העדכון (נייד ומותקן) ממתינים עכשיו דרך **PowerShell
`Get-Process`** — קודם ל-PID שלנו (עד 30 שניות), אחר כך לכל עותק אחר לפי שם
(עד 10 שניות), ואם משהו עדיין חי — `Stop-Process -Force` ורישום ל-`update.log`.
סגירה בכוח מוצדקת כאן: לתהליך שנשאר אין חלון ואין מה לשמור, והוא החוסם היחיד.

**אומת בפועל** מול תהליך שמחזיק את אותו mutex: `tasklist` דיווח MISSING,
ה-PowerShell זיהה, סגר, רשם ליומן, ושלב ההתקנה נמשך.

**לקח**: `tasklist /FI` אינו מנגנון אמין לבדיקת חיים. `Get-Process` כן. ואל
תסמוך על לולאת המתנה שלא ראית נכשלת — כאן היא "עברה" שנים בלי לעשות כלום.

## מה תוקן ב-0.8.13 (2026-09-04) — ההתקנה נכשלה כשהתוכנה פתוחה

**דיווח**: גם התקנה ידנית נכשלה — `DeleteFile נכשל; קוד 5. Access is denied`.

**השורש**: התוכנה הייתה **פתוחה**. מחיקת קובץ הרצה מחזירה `ERROR_ACCESS_DENIED`
(5) ולא "בשימוש על ידי תהליך אחר" (32), כי דמות ה-EXE ממופה לזיכרון — ולכן
ההודעה גם לא רמזה למשתמש מה לעשות. אומת: התהליך רץ (PID 30832), ההרשאות על
הקובץ תקינות (FullControl), והוא אינו לקריאה בלבד.

**`CloseApplications=yes` לבדו לא הספיק.** הוא נשען על Restart Manager, וביומן
ההתקנה נראה במפורש: `Some applications could not be shut down` — RM זיהה את
התוכנה אבל לא הצליח לסגור אותה (חלון PyWebView לא נענה לבקשה בזמן).

**תוקן:**
- **`AppMutex=TikNick-single-instance`** — הבדיקה נעשית לפני שנוגעים בקבצים,
  ומצביעה בדיוק על המנעול ש-`_single_instance_or_exit` יוצר. אומת שהשם ללא
  קידומת נפתר לאותו אובייקט כמו `Local\TikNick-single-instance` באותו session.
- **`[Messages] hebrew.SetupAppRunningError`** — הודעה שאומרת מה לעשות
  ("סגור אותה לגמרי... ואז לחץ אישור") במקום שגיאת DeleteFile גולמית.
- **העדכון השקט מריץ את ה-Setup רק אחרי שהתוכנה נסגרה.** ב-0.8.12 הוא הורץ
  במקביל עם `sleep(1.5)` לפני היציאה — וזה בדיוק המצב שנכשל: RM רואה תהליך חי
  ומבטל. עכשיו batch ממתין ל-PID (עד 60 שניות) ורק אז מריץ, כמו במסלול הנייד.

**נמדד בפועל** מול התקנה חיה עם התוכנה פתוחה: ההתקנה נעצרת ומגלגלת אחורה
(`Rolling back changes` / `Uninstallation process succeeded`) במקום להשאיר
התקנה חצי-מוחלפת. הגרסה המותקנת נשארה שלמה.

## מה תוקן ב-0.8.12 (2026-09-04) — העדכון העצמי

**דיווח מבנימין**: עדכון מ-0.8.3 ל-0.8.11 "נופל אחרי ההורדה".

### מה קרה באמת
היומן (`tiknick.log`) סיפר את הסיפור: ב-02:40 רץ העדכון, התוכנה יצאה (בלי
"closed normally" — היא משתמשת ב-`os._exit`), וב-02:44 היא חזרה **עדיין
ב-0.8.3**, בלי שורת שגיאה אחת. ההתקנה של בנימין היא
`%LOCALAPPDATA%\Programs\Tik-Nick` — כלומר **מסלול האינסטולר**, לא הנייד.
`TikNick-Setup-new.exe` נמצא ב-TEMP, וה-SHA-256 שלו **זהה** לנכס ששוחרר:
ההורדה הייתה מושלמת, וה-Setup השקט הוא שנכשל.

### למה זה היה בלתי ניתן לאבחון
מסלול האינסטולר היה שורה אחת:
`Popen([setup, "/SILENT", "/SUPPRESSMSGBOXES", "/NOCANCEL", "/CLOSEAPPLICATIONS"])`
ואז `os._exit(0)` מיידי. שלוש בעיות:
1. **בלי `/LOG`** — Inno לא השאיר שום עקבה, ו-`/SUPPRESSMSGBOXES` בלע גם את
   הודעת השגיאה.
2. **בלי ניקוי סביבה** — ה-Setup ירש את משתני `_MEI*`/`_PYI*` של PyInstaller.
   המסלול הנייד מנקה אותם במפורש; מסלול האינסטולר לא.
3. **`os._exit(0)` מיידי** — התהליך נעלם באותו רגע שה-Setup עלה, בזמן ש-
   `/CLOSEAPPLICATIONS` מחפש אותו.
4. **בלי סימון** — התוכנה חזרה בגרסה הישנה, וזה נראה בדיוק כמו "לא קרה כלום".

### מה תוקן
- **`/LOG` ל-`<data_dir>/update-inno.log`**, סביבה נקייה, `DETACHED_PROCESS`,
  ו-1.5 שניות לפני היציאה כדי שה-Setup יספיק להיאחז.
- **`update-pending.txt`** נכתב לפני הניסיון עם הגרסה שאליה מעדכנים. בהפעלה
  הבאה, אם הגרסה **לא** השתנתה — נפתח דיאלוג שאומר את זה במפורש, עם כפתור לדף
  הגרסאות וכפתור לפתיחת יומן ההתקנה.
- **המסלול הנייד**: `move /Y new cur` הוחלף ב**שינוי שם הצידה ואז הכנסה**.
  Windows מרשה לשנות שם ל-EXE רץ אבל **לא לדרוס אותו** — זה מסביר כישלון החלפה
  שקט שקורה כשאנטי-וירוס סורק את ההורדה או שסייר הקבצים פתוח על התיקייה. אם
  שלב ההכנסה נכשל, הישן מוחזר; והכול נרשם ל-`update.log`.

### 📋 תקציר שינויים שמתאים את עצמו לגודל הקפיצה
`changelog.json` בשורש הריפו — יומן קריא-למכונה, כל פריט מתויג
`major`/`feature`/`fix`. `get_update_summary()` מושך אותו מ-
`raw.githubusercontent.com` ומנסח לפי מרחק הגרסאות:
- **גרסה אחת** → פירוט מלא, מקובץ לחשוב/חדש/תיקונים.
- **2–3 גרסאות** → החשוב והחדש במלואם, התיקונים כמספר אחד.
- **4+ גרסאות** → כותרות בלבד, והתקרה **מתהדקת ככל שהקפיצה גדלה**
  (3 major + 4 feature עד 6 גרסאות; 2 + 3 מעבר לזה).

זה הכלל המרכזי, והוא נבדק: **קפיצה של 9 גרסאות מפיקה תקציר קצר יותר מקפיצה של
2** (6 שורות מול 5+ — וצפיפות של 0.7 שורות לגרסה מול 2.5). מי שדילג על חצי
שנה לא יקרא קיר טקסט. נכשל בשקט — אם היומן לא נטען, הדיאלוג מציג את הערות
ה-Release כמו קודם.

**בדיקות**: `tests/test_update.py` (25) — כולל שהגרסה הנוכחית מופיעה ביומן,
שהיומן ממוין, ושהתקציר מתקצר עם הקפיצה.

## מה תוקן ב-0.8.11 (2026-09-03) — ביקורת אדוורסרית על 0.8.6–0.8.10

**מקור**: 8 עדשות על *הקוד שנוסף בחמש הגרסאות של אותו יום* — קוד שרק מי שכתב
אותו קרא. 50 ממצאים, 46 שרדו אימות מקובץ (כל מאמת קיבל קבוצה שלמה להפריך, ולא
ממצא בודד — כך שלב האימות לא נופל שוב על מגבלת סשן), ועוד 7 ממבקר השלמות.
כמעט כולם הגיעו עם שחזור בפועל.

### 🚨 חוסם שחרור: מאגר מ-0.8.8 לא נפתח
`init_db` יצר את `idx_recent_seq` בתוך אותו `executescript` של יצירת הטבלאות.
במאגר שנוצר ב-0.8.8 הטבלה `recent_views` כבר קיימת **בלי** עמודת `seq`,
`CREATE TABLE IF NOT EXISTS` הוא no-op, והאינדקס נכשל — כלומר **כל `init_db`
נפל, והתוכנה פשוט לא נפתחה** למי שהתקין 0.8.8 ועדכן. האינדקס עבר ל-`_migrate`,
אחרי ה-`ALTER TABLE`. **הכלל: אינדקס על עמודה שנוספת במיגרציה חייב להיווצר
במיגרציה, לעולם לא בסקריפט היצירה.**

### 🚨 שחזור שהרס את המאגר החי
`restore_from` דרס את המאגר ואז קרא ל-`init_db()` בלי הגנה. גיבוי שעובר
`integrity_check` אבל לא עובר מיגרציה השאיר את המשתמש בלי הנתונים הישנים *וגם*
בלי החדשים, בזמן שהממשק אומר "השחזור נכשל". עכשיו: `_backup_migrates_ok()`
בודק את הקובץ על **עותק בתיקייה זמנית לפני** שנוגעים במאגר החי, ואם בכל זאת
נכשל — עותק הבטיחות מוחזר אוטומטית.

### 🚨 הזרקת `nick_color` — עוד שני מקומות
0.8.8 סגר ארבעה ותיעד "ארבעת מקומות ההזרקה". היו שישה:
- `buildCardElement` — תצוגת הכרטיסים, כלומר הדליפה נשארה פתוחה בכל גלילה בה.
- `removeAvatar` — ומזריק ל**מסמך הראשי**, שם `pywebview.api` חי. נבנה מחדש
  בלי `innerHTML` בכלל.
`esc()` לא מספיק: הוא משאיר `; : ( )` על כנם, כך ש-`red;background-image:url(…)`
נשאר תקף בתוך אותו `style` והופך לבקשה חיצונית. **הכלל: ערך מהפורום בתוך
`style=` עובר `safeColor`, לא `esc`.** אומת בדפדפן.

### תיקונים נוספים
- **`profile_sheet` השאיר את `__TITLE__` בלי בריחה** — שם משתמש יכול היה לסגור
  את `<title>` ולהחדיר `<img src=https://…>` לגיליון המודפס, בדיוק ההפניה
  החיצונית שהמודול מבטיח שאין בו.
- **איפוס שהגיבוי שלפניו נכשל היה מתבצע בכל זאת.** `auto_backup` לא מרימה
  חריגה במכוון, והתוצאה נזרקה — כלומר רשת הביטחון הייתה מראית עין דווקא כשהיא
  נדרשת. עכשיו מוחזר `needs_confirm` והמשתמש מחליט.
- **גיבוי יומי שנקטע** (thread daemon + סגירת החלון) השאיר קובץ חלקי שנספר
  כגיבוי תקין ואף *פינה מקום* לגיבוי טוב שנמחק. נכתב ל-`.part` ומשנה שם רק
  כשהוא שלם; `list_backups` מדלג על קובץ עם `-wal`/`-journal` לצידו.
- **`restore_db` השאיר `_sched_hold` דלוק לנצח** כשהמשתמש ביטל את דיאלוג
  הקובץ — התזמון מת בשקט עד סוף הריצה. ה-hold עוטף עכשיו גם את הדיאלוג.
- **התזמון הציע פורומים שהסורק מסרב להם** (XenForo/phpBB). כל טיק ספר עוד
  כישלון, וחמש דקות אחר כך התזמון כולו כבה בגלל פורום אחד. מסונן בשני מקומות.
- **"⏭ דלג" ו"עצור" בסריקה אוטומטית יצרו לולאה**: שניהם לא כותבים
  `last_scrape`, אז הפורום נשאר "מגיע לו" והטיק הבא הפעיל אותו שוב בעוד דקה.
  שניהם מנמנמים עכשיו 6 שעות. וסריקה ש**נעצרה** אחרי 5 עמודים כושלים נספרה
  כהצלחה ואיפסה את מונה הכישלונות — עכשיו נספרת ככישלון.
- **`_scrape_state["auto"]` לא התאפס בסוף ריצה**, כך שביטול *ידני* מאוחר יותר
  נימנם את התזמון ל-6 שעות בלי סיבה.
- **שינוי שם פורום יתם את ההגדרות שלו**: `forumio_<שם>` (החרגה מייצוא) ו-
  `last_scrape_<שם>`. פורום שהוחרג חזר לייצא בשקט — עם אנשי הקשר שלו, מגרסה 3 —
  ורצפת 12 השעות נעקפה. `_rename_forum_settings` מעביר את שניהם ואת הרשומה
  ב-`sched_forums`.
- **דוח ההשוואה אמר את ההפך מהאמת** כשהופעלה הגבלת פוסטים: המסקנה חושבה מכמות
  ה**נסרקת**, שההגבלה חותכת לשני המשתמשים לאותו מספר. עכשיו לפי `postcount`
  האמיתי, עם ציון מפורש של הבסיס. וכל ערך שמקורו בפורום עובר `esc` — הדוח
  החד-משתמשי עשה זאת, ההשוואה לא.
- **קידוד UTF-16 בלי BOM נבחר לפי בית 1** — בקובץ שמתחיל באות עברית שני הבתים
  אינם אפס, אז הניחוש יצא הפוך וכל הקובץ נקרא כג'יבריש. גם ספירת בתי אפס לא
  עובדת: לעברית ב-UTF-16 אין בתי אפס כלל. עכשיו מפענחים לשני הכיוונים ומנקדים
  את התוצאה. שבעת מסלולי הקידוד מכוסים בבדיקות.
- **תיקון האפס המוביל** הוסיף אפס גם למספרים בני 9 ספרות שאינם קידומת ישראלית.
- **שתי עמודות CSV לאותו שדה** נבלעו בשקט, ומי מהן ניצחה השתנה משורה לשורה.
- **קובץ `.tiknick` עם BOM** נכשל בפתיחה עם שגיאת מפענח באנגלית.

### מדידה: האינדקס `idx_nicks_forum_username` נשאר
נמדד בבידוד: הוא **מייקר ב-37% הכנסה המונית ראשונה** (20 אלף ניקים: 6.15s →
8.41s) אבל **מוזיל ייבוא ב-18%** (1003ms → 821ms), והוא משרת גם את השליפה
החמה של `merge_scraped_users`. סריקה חוזרת — המקרה השכיח — לא הושפעה (11ms
ל-500 משתמשים). זו עסקה נכונה; העלייה שנראתה בבנצ'מרק היא מחירו, לא רגרסיה.

**בדיקות**: `tests/test_audit_0811.py` (38).

## מה נוסף ב-0.8.10 (2026-09-03) — שני האחרונים מהמפרטים

**הערה על מספור**: אחרי 0.8.9 בא **0.8.10** ולא 0.9. בדקתי שבדיקת העדכונים
עומדת בזה — `check_for_updates` מפרק למספרים (`parse()` ב-main.py) ולכן
`0.8.10 > 0.8.9`. השוואת מחרוזות הייתה נותנת `"10" < "9"` ושוברת את העדכון
לכל המשתמשים בשקט.

### ⏰ סריקה מתוזמנת
זו התכונה היחידה בתוכנה שפונה לשרתים של אחרים **בלי שהמשתמש נוכח**. הפורומים
האלה הם התקנות NodeBB קטנות שמתנדבים מתחזקים, חלקן על Render חינמי שנרדם
ומתעורר לאט. לכן העיצוב כולו רצפות, וכל ברירת מחדל היא בצד הזהיר:
- **כבוי כברירת מחדל** (`sched_enabled='0'`), **ורשימת הפורומים ריקה** — הדלקת
  המתג לבדה עדיין לא סורקת כלום; צריך לסמן כל פורום.
- **`SCHED_MIN_INTERVAL_HOURS = 12`, נאכף פעמיים**: פעם בכתיבה (`set_schedule`
  נצמד למינימום) ופעם בהחלטה (`sched_due_forums` בודק מול `last_scrape_<forum>`
  האמיתי) — כך ששורת הגדרות שנערכה ידנית לא יכולה לרדת מתחת לרצפה.
- ברירת מחדל: פעם ביום ב-03:00. `SCHED_STARTUP_GRACE_SEC=300` — פתיחת התוכנה
  לא מפעילה סריקה מיידית. הטיק כל 60 שניות **רק מחליט**, ומעביר ל-
  `start_scrape_all` הקיים עם ההשהיות, ה-UA המזוהה וכיבוד Retry-About שלו.
- **`cookie=""` במכוון** — `start_scrape_all` קורא עוגייה שמורה לכל פורום
  בנפרד. כלל הפרטיות מ-0.8.3 (עוגייה של פורום אחד לא נשלחת לאחר) לא נחלש,
  ולא נכתב כאן שום קוד עוגיות חדש.
- **לולאת ביטול-הפעלה**: ביטול לא כותב `last_scrape`, ולכן הפורום נשאר "מגיע
  לו" והטיק הבא היה מפעיל אותו שוב בעוד דקה — לולאה שנוצרת דווקא מכפתור
  העצירה. `cancel_scrape` מציב `sched_snooze_until = +6h` כשהריצה אוטומטית.
- **`_sched_hold`** סביב `vacuum`/`restore`/`reset`: אלה לא מרימים דגל
  "running", ולכן `_busy()` לא כיסה אותם והטיק היה יכול להתחיל סריקה לתוכם.
- **5 כישלונות רצופים עוצרים את התזמון** (`sched_note_result`), עם הודעה
  חד-פעמית (`sched_pop_notify`) — לא נודניק אינסופי לפורום שנפל.
- **UTC מול שעון מקומי**: `last_scrape_<forum>` נכתב ב-`utcnow()`, ואילו שעת
  היעד היומית היא שעון מקומי. השוואה של השתיים באותה מערכת ניפחה את "כמה עבר"
  בהפרש אזור הזמן — בישראל 2-3 שעות, מספיק כדי לרוץ מוקדם מהרצפה.
- **שקט**: ריצה אוטומטית לא מציגה toast כשלא השתנה כלום, ולא טוענת מחדש את
  הטבלה כשחלון פתוח (מציעה "🔄 רענן" במקום למשוך את הרשימה מתחת לידיים).

### 👥 השוואת שני משתמשים ב-Chazonishnik
שדה "משתמש שני" בדיאלוג הקיים; מילוי שלו מפיק דוח השוואה במקום ניתוח יחיד.
- **הסריקות רצות בזו אחר זו ולא במקביל.** שתיים בו-זמנית מכפילות את העומס על
  אותו פורום, וכל מנגנון הנימוס בנוי סביב זרם אחד.
- `_collect()` הוצא מ-`analyze_user` כדי ששני המסלולים ישתמשו **באותו** קוד
  רשת — כולל הריטריי וההשהיות — ולא ייווצר נתיב שני.
- **חלקיות מדווחת לכל משתמש בנפרד**: "א' הושלם, ב' נעצר" הוא מצב אמיתי.
- הדוח: טבלת מספרים, שלושה גרפים מזווגים (שעות/ימים/לאורך זמן) וסיכום במילים
  ("מי כותב יותר", "מי מקבל יותר לייקים", "שעות שיא שונות"). אותו `_esc` ו-
  `_json_for_script`, ואותו Chart.js מוטמע — הדוח עובד אופליין.
- אותו `_chz_state` ואותו כפתור ביטול; ההתקדמות מוסיפה "משתמש 1 מתוך 2".

**בדיקות**: `tests/test_schedule.py` (43) — רובן על הרצפות. אומת גם בדפדפן
דרך `tests/harness.py`: שני הדיאלוגים נפתחים, המתג כבוי כברירת מחדל, ושדה
השעות נושא `min="12"`.

**בזה נסגרו כל עשרת המפרטים** שנכתבו ב-0.8.7.

## מה נוסף ב-0.8.9 (2026-09-03) — להשלים את מה שהממשק כבר מבטיח

**מקור**: הצלבה שיטתית של מה שהתוכנה *מצהירה* מול מה שהיא *עושה* — מתודות API
שאיש לא קורא להן, הגדרות שנשמרות ולא נקראות, טבלאות עם קוד מלא ובלי ממשק,
ושדות שנאספים ואי אפשר לראות. משהו שהממשק מבטיח ולא מקיים גרוע מתכונה שאינה
קיימת.

### מה היה מוצהר ולא עבד
- **פאנל "מידע לפי מקור" הציג את המנצח הלא נכון.** ב-0.8.6 נוסף `is_winner`
  בצד השרת, וה-JS המשיך לקחת את `srcs[0]` לפי מיון אמינות — בדיוק ההנחה
  שהתיקון בא לבטל (לסטטוס ולמוניטין יש כללים משלהם). תוקן, וערכים זהים ממקורות
  שונים מקובצים לשורה אחת.
- **הפאנל היה קריאה-בלבד** והפנה את המשתמש ללחצן עקיף ("שנה אמינות מקור").
  נוסף **"השתמש בזה"** → `pick_field_value`: רושם את הערך תחת מקור "אני" ומכריע
  מחדש, בלי למחוק את שאר הערכים. שדה שיש לו מקור אבסולוטי (סטטוס מסריקה) מחזיר
  **הסבר בעברית** במקום להיכשל בשקט.
- **"האמינות שלך"**: דיאלוג הייבוא כתב מפורשות "ניתן לשינוי בהגדרות סנכרון" —
  ושם לא היה שום פקד. `set_my_trust` היה קיים ב-API ולא נקרא מעולם. נוסף מחוון,
  והוא גם מריץ הכרעה מחדש (מקור "אני" הוא מקור ככל אחר).
- **`shelved_values`** — טבלה, `get_shelved_values`, `promote_shelved`, שחזור
  בסל המחזור, ותיעוד ב-CLAUDE.md — **ובלי שום ממשק**. נוסף מקטע "📥 ערכים על
  המדף" בדיאלוג הניק. `promote_shelved` גם תוקן: הוא כתב ישירות ל-cache בלי
  הכרעה (אותה משפחת באגים שתוקנה ב-0.8.6).
- **`import_sources`** — כל ייבוא נרשם שם מאז הגרסאות הראשונות ומעולם לא הוצג.
  נוסף "📥 יומן ייבואים".
- **שדות שנאספים ואי אפשר לראות**: `join_date` היה *סינון* בלי עמודה,
  ו-`post_count`/`trust_level` לא היו לא עמודה ולא סינון. שלושתם נוספו כעמודות
  ו-`post_count`/`trust_level` גם כשדות סינון.

### 📐 רוחב וסדר עמודות
`col_layout` ב-display settings: `{"order":[keys],"w":{key:px}}`.
- **`<colgroup>` הוא מקור האמת לרוחב תחת `table-layout:fixed`** — בלעדיו השורה
  הראשונה ב-tbody קובעת, והיא לרוב ה-spacer של הגלילה הווירטואלית.
- **גיאומטריית RTL, שלושה כללים שאסור לטעות בהם**: הידית יושבת על
  `left:0` (ב-RTL עמודה מתחילה בימין ו**מסתיימת** בשמאל); הדלתא היא
  `startX - clientX` — **הפוך מ-LTR**, כי גרירה שמאלה מרחיבה; וכל בדיקת המיקום
  היא `getBoundingClientRect` + `clientX` בלבד, כי `#table-wrap` הוא גולל RTL
  ש-`scrollLeft` שלו **שלילי** בכרומיום. קו השחרור הוא `position:fixed` על
  `<body>` ולכן לא צריך להתחשב בגלילה בכלל.
- טעינה סלחנית: JSON פגום, מפתח לא מוכר, כפילות או עמודה חסרה → ברירות מחדל
  בלי חריגה; עמודה חדשה בגרסה עתידית מתווספת בסוף ולא שוברת פריסה שמורה.
- לחיצה קצרה עדיין ממיינת; גרירה מעל 5 פיקסלים לא. שמירה שקטה 250ms אחרי
  השחרור — אין קריאת גשר באמצע הגרירה. `autoFitColumn` מודד **רק שורות שנבנו
  ב-DOM** (עשרות), לעולם לא את `S.nicks`.
- "↺ אפס רוחב וסדר עמודות" בהגדרות התצוגה, עם "**לא נוגע בנתונים**" — כי
  בדיאלוג האיפוס יש "🧹 אפס עמודות נבחרות" שמוחק ערכים ב-90 אלף ניקים.

### 🧪 ארגז חול לממשק — `tests/harness.py`
CLAUDE.md ציין "אין בדיקות UI", והתוכנה היא חלון PyWebView שאי אפשר לפתוח בלי
מסך. `python tests/harness.py` מגיש את `web/` האמיתי מול גשר `pywebview` מזויף
עם נתוני דמה, כך שאפשר לראות ולנהוג בטבלה בדפדפן. עם זה אומתו בפועל: התאמת
מספר התאים לכותרת, סימן שינוי הרוחב, קו השחרור, שמירה, ושלחיצה עדיין ממיינת
וגרירה לא. `tests/test_columns.js` בודק את הפונקציות הטהורות ב-node, ו-
`run_all.py` מריץ עכשיו גם חבילות `test_*.js`.

**תוקן תוך כדי**: `recent_views` מיין לפי חותמת זמן, וגם ברזולוציית מילישנייה
שתי צפיות רצופות קיבלו אותו ערך והגיזום היה שרירותי. עבר למונה עולה (`seq`),
עם מיגרציה למאגר שנוצר ב-0.8.8.

**מה נשאר**: סריקה מתוזמנת, השוואת שני משתמשים ב-Chazonishnik.

## מה נוסף ב-0.8.8 (2026-09-03) — לראות את המידע

שלוש התכונות שנשארו מהמפרטים והן כמעט לגמרי קריאה-בלבד, ולכן נבנו ונבדקו יחד.

### 🗺️ מפת זהויות — `get_identity_map()`
כל קבוצות הזהות במבט אחד. **שתי שאילתות בלבד**: אחת על `nick_identities` ואחת
על הניקים שמופיעים בה — לא סורק את טבלת 90 האלף, ולא מחזיר `avatar_image`
(כלל מטען הרשימות מ-0.8.3).
- **הקיבוץ הוא union-find ולא הנחת קליקה.** הקוד שומר כל זוג, אבל קבוצה יכולה
  לאבד קישור אחרי שחזור חלקי מסל המחזור, ואיחוד לפי צמתים נכון לכל צורת גרף.
  `repair_identity_groups()` משלים את הזוגות החסרים (כפתור "🔧 תקן קבוצות זהות"
  ב"בריאות המאגר"), ואידמפוטנטי.
- `conflicts` — שדות שחברי הקבוצה חלוקים עליהם (שם אמיתי/טלפון/מייל/שם מלא).
  זה האות היחיד שבאמת פועלים לפיו: או קישור שגוי, או ידיעה חדשה.
- הגליף הוא SVG בעבודת יד (נקודה לחבר על מעגל + מיתרים) — אין ספריית גרפים
  בחבילה, ורשימה מקובצת קריאה יותר ממאות צמתים צפים.
- ניתוק חבר מציג "↩ בטל" שמחזיר את הקבוצה בדיוק כפי שהייתה.

### 🖨️ פרופיל להדפסה — `profile_sheet.py`
**אין מסלול הדפסה בתוך התוכנה, וזו לא בחירה סגנונית:** ה-iframe של הדוחות הוא
`sandbox="allow-scripts allow-popups"` בלי `allow-modals`, ולכן `window.print()`
בתוכו הוא no-op שקט; קריאה ל-`contentWindow.print()` מבחוץ זורקת SecurityError
(origin אטום); ו-pywebview רץ עם `debug=False`, שמכבה את Ctrl+P ואת תפריט
ההקשר. **אין להוסיף `allow-modals` כדי "לתקן"** — זה היה נותן alert/confirm/print
לכל דוח שמקורו בפורום.
לכן: פייתון כותב את הגיליון ל-`<data_dir>/print/` ומוסר אותו ל-`os.startfile`,
בדיוק כמו "פתח תיקיית נתונים". הדפדפן האמיתי מריץ את `window.print()` שבקובץ,
ושם יש גם "Microsoft Print to PDF".
- **`open_url` לא נגעו בו.** המתודות מקבלות מזהה בלבד — לא נתיב ולא HTML — כך
  ש-JS לא יכול להשפיע על מה שנפתח.
- **אפס הפניות חיצוניות בגיליון.** תמונה נכנסת רק אם היא `data:image`;
  `avatar_url` מרוחק מודפס כטקסט. אחרת עצם ההדפסה הייתה מודיעה לפורום שאספת
  תיק על המשתמש הזה.
- `include_private` כבוי כברירת מחדל; כשמדליקים, בתחתית הגיליון "⚠️ כולל הערות
  אישיות". שם הקובץ לפי **מזהה** ולא לפי שם משתמש (הדפדפן מדפיס את הנתיב
  בכותרת), והתיקייה נגזמת ל-40 קבצים / 24 שעות ונוספה ל-.gitignore.
- ההחלפה בתבנית היא **מעבר regex אחד** ולא `.replace()` בשרשרת, כך שטקסט של
  משתמש שמכיל `__FIELDS__` לא נסרק שוב (מלכודת שקיימת ב-stinknik).
- הכפתור בדיאלוג הניק **לא פותח חלון** — `openModal` סוגר את הנוכחי כשלב ראשון
  ואין לו מחסנית, כך שחלון נוסף היה מוחק טופס עריכה פתוח. משם מדפיסים ישירות;
  דיאלוג האפשרויות + תצוגה מקדימה נפתח רק מהפרופיל המאוחד.

### 🕘 נצפו לאחרונה + דירוג רלוונטיות
- טבלה `recent_views` (ולא JSON בהגדרות): `CASCADE` מנקה לבד ניק שנמחק, ואין
  מרוץ בין שני כותבים. `PRIMARY KEY` על `nick_id` = צפייה חוזרת מקדמת ולא
  מכפילה. **`strftime('%Y-%m-%d %H:%M:%f')` ולא `datetime('now')`** — ברזולוציית
  שנייה, רצף צפיות מהיר היה נגזם באקראי במקום לפי סדר.
- **דירוג חיפוש**: `has_info DESC` נשאר בדיוק כשאין חיפוש (מי שמדפדף רואה קודם
  את מה שהעשיר), אבל **עם** מחרוזת חיפוש הרלוונטיות גוברת: התאמה מדויקת בשם
  המשתמש → תחילית → הכלה → שם אמיתי/מלא → השאר, ו-`has_info` הופך לשובר שוויון.
  קודם, חיפוש "לומדעס" יכול היה להחזיר קודם ניק אחר שרק מזכיר אותו בהערות.

**גם**: `safeColor()` ב-app.js. `nick_color` מגיע מהפורום, ו-`esc()` מנע בריחה
מהמאפיין אבל לא ערך כמו `red;background-image:url(https://forum/track.png)` —
שהיה נשאר תקף בתוך אותו `style` והופך לבקשה חיצונית מה-WebView בכל צפייה
בפרופיל, כלומר הפורום לומד שאתה מסתכל על המשתמש הזה. עכשיו רשימה לבנה של hex
או שם צבע, בכל ארבעת מקומות ההזרקה.

**בדיקות**: `tests/test_views.py` (38).

**מה נשאר**: רוחב/סדר עמודות (RTL, 17 שינויים), סריקה מתוזמנת, השוואת שני
משתמשים ב-Chazonishnik.

## מה נוסף ב-0.8.7 (2026-09-03) — "הנתונים שלך"

**מקור**: 9 מפרטי תכנון (סוכן לכל אשכול, קריאה-בלבד מול הקוד). שלב האינטגרציה
שלהם נפל על מגבלת סשן, ולכן הקיצוץ נעשה ידנית: מתוך עשרה מפרטים שכולם אמרו
"לבנות" (182 שינויים) נבחרו ארבעה שחיים באותו אזור קוד ונבדקים יחד. השאר
ממתינים במפרטים המלאים (ראו "מה נשאר").

### פורמט `.tiknick` גרסה 3 — אנשי קשר וזהויות נוסעים עם הקובץ
עד 0.8.6 הייצוא כלל **רק עמודות של הניק**, כך ששיתוף קובץ איבד בדיוק את מה
שהמשתמש עבד עליו הכי קשה: אנשי הקשר הנוספים וקישורי הזהות.
- `EXPORT_FORMAT_VERSION = 3`. הקובץ מקבל `contacts` בתוך כל ניק, `identity_groups`
  ברמה העליונה, ו-`counts` לדיווח.
- **`exported_fields` ממשיך להכיל שמות עמודות בלבד** — זו ההבטחה שמחזיקה את
  התאימות: גרסה ישנה קוראת `nicks[]`/`exported_fields` ומתעלמת מהשאר, וקובץ
  גרסה 2 מיובא כרגיל.
- **קבוצות זהות מיוצאות כ-(פורום, שם משתמש)**, לא כ-id — id חסר משמעות במאגר
  אחר. קישור נכלל רק אם **שני** צדדיו יוצאו, אחרת הקובץ היה חושף פורום+שם של
  ניק שהוחרג במכוון. בייבוא, קישור שצדו השני לא קיים מדולג **ומדווח**.
- **`is_private=1` לעולם לא יוצא** — מסונן ב-SQL, לא בפייתון, ואינו תלוי בשום
  מתג. אנשי קשר שנקלטים מקובץ נכנסים תמיד גלויים ("סודי" הוא סימון אישי של מי
  שקלט, לא משהו שקובץ מבחוץ קובע).
- דה-דופליקציה לפי `_contact_key`: `050-123-4567`, `0501234567` ו-`+972501234567`
  הם אותו איש קשר, כך שייבוא חוזר של אותו קובץ לא מכפיל דבר.
- `EXTRA_SYNC_KEYS` (contacts/identities) יושבים ב-`sync_settings` אבל **לא**
  ב-`ALL_NICK_FIELDS` — כל צרכני `ALL_NICK_FIELDS` מניחים "שם עמודה בטבלת nicks".
  שני מתגים בהגדרות הסנכרון, ושתי תיבות סימון בדיאלוג הייבוא.
- `_identity_group_many` (BFS על קבוצת זרעים, frontier במנות של 400 — מגבלת
  999 הפרמטרים של SQLite) ו-`_link_identity_group_conn` (קליקה על חיבור קיים,
  כדי שהייבוא לא יקרא ל-`add_identity` פר-זוג).

### ייבוא CSV / TSV — `csv_import.py` (מודול חדש, פונקציות טהורות)
מייצר בדיוק את המבנה ש-`db.import_data` מקבל, כדי שקובץ אקסל יעבור באותו מנוע
מקורות (מקור ייבוא + אמינות + הכרעה) ולא ייכתב ישירות לטבלה.
- **סדר פענוח הקידוד קריטי**: BOM ← UTF-16 בלי BOM ← UTF-8 **קפדני** ← cp1255.
  cp1255 מפענח כמעט כל רצף בתים בלי לזרוק שגיאה, ולכן חייב להיות אחרון — אחרת
  קובץ UTF-8 עברי מתפענח בשקט לג'יבריש.
- **לא `csv.Sniffer`** (נופל על פסיקים וגרשיים בטקסט עברי חופשי): רשימת מועמדים
  קבועה `\t , ; |`, והזוכה הוא זה שנותן הכי הרבה עמודות בעקביות.
- `unquote_cell` מבטל בדיוק את מה ש-`export_csv` עשה: `="0501234567"` והקידומת
  `'` שלפני `= + - @`. גרש שאינו לפני תו נוסחה נשאר (ז'אנר, ר' משה).
- **קובץ שהתוכנה עצמה ייצאה ממופה ב-100% בלי קליק** — `export_csv` כותב את
  התוויות של `ALL_NICK_FIELDS`, ו-`auto_map` בונה אינדקס מאותה רשימה + כינויים
  נפוצים (ניק/נייד/מייל/שם).
- `_BLOCKED`: `trust_level`, `source`, `scraped_*` לא ניתנים לייבוא מ-CSV —
  האמינות נקבעת ע"י מקור הייבוא.
- אקסל בולע אפס מוביל: `501234567` → `0501234567` (מתג בממשק).
- כפילות של אותו (פורום, שם משתמש) בתוך הקובץ מאוחדת לרשומה אחת.

### תצוגה מקדימה לייבוא — `preview_import`
הייבוא אינו הפיך (רק למחיקת ניקים יש סל מחזור), ולכן **כל** ייבוא עובר עכשיו
דרך דוח יבש: כמה ניקים חדשים מול קיימים, כמה ערכים ייכתבו, כמה יתנגשו עם ערך
קיים שונה (עם 12 דוגמאות "קיים מול מהקובץ"), ומה יידלג ולמה (בלי שם משתמש /
פורום שכובה בהגדרות). רץ ב-thread עם התקדמות דרך `_import_state`.
עלות: מעבר אחד על רשומות הקובץ + שליפת הקיימים במנות של 400 — אותה עבודה
שהייבוא עושה ממילא בשלב הטעינה, לא מעבר שני על המאגר.

### גיבוי אוטומטי
- `auto_backup(reason, force)` דרך ה-backup API של SQLite (לא העתקת קובץ — WAL).
  יומי בהפעלה **ב-thread** (88MB לוקחים כמה שניות), ולפני כל איפוס.
- **תקרות אמיתיות כי התיקייה על C: שצפוף**: `AUTO_BACKUP_KEEP=3`,
  `AUTO_BACKUP_MAX_BYTES=1GB`, `AUTO_BACKUP_MIN_HOURS=20`. הגיזום ממיין לפי
  `st_mtime` **float** ואז לפי שם — שני גיבויים באותה שנייה חייבים סדר יציב,
  אחרת נמחק אחד מהם באקראי במקום הישן.
- `auto_backup` **לעולם לא מרימה חריגה**: דיסק מלא או נתיב חסום לא ימנעו
  מהמשתמש לעבוד. פאנל בדיאלוג "🩺 בריאות המאגר" עם מתג, רשימה, נפח ו"גבה עכשיו".

**בדיקות**: `tests/test_portability.py` (41). `run_all.py` מעביר עכשיו
`PYTHONIOENCODING=utf-8` לתת-התהליכים — בלעדיו כל `print` עם עברית או אמוג'י
מפיל את החבילה ב-`UnicodeEncodeError` שאין לו קשר לנבדק.

**מה נשאר מהמפרטים** (מוכנים ליישום): מפת זהויות (לוח מקובץ, לא גרף — כי
הקבוצות שמורות כסגור טרנזיטיבי מלא), פרופיל להדפסה, "נצפו לאחרונה" + דירוג
רלוונטיות בחיפוש, רוחב/סדר עמודות (RTL, 17 שינויים), סריקה מתוזמנת (בזהירות —
לולאה לא מפוקחת מול פורומים אמיתיים), והשוואת שני משתמשים ב-Chazonishnik.

## מה תוקן ב-0.8.6 (2026-09-03) — גרסת נכונות ויעילות

**מקור**: ציד באגים ב-7 עדשות (65 ממצאים גולמיים). שלב האימות האדוורסרי נפל על מגבלת
סשן, ולכן כל ממצא אומת ידנית — רובם **אמפירית מול מאגר זמני**, לא בקריאה בלבד. מה
שלא הצליח לשחזר לא נכנס. כל תיקון כאן מכוסה ב-`tests/test_integrity.py`.

**ה-cache והמנוע נפרדו זה מזה בארבעה מקומות — זו המשפחה החשובה:**
`nicks` הוא cache; האמת ב-`field_values`. כשכותבים לאחד בלי להכריע מחדש, המשתמש רואה
דבר אחד והמאגר מחזיק אחר.
- `bulk_update_field` עם ערך ריק הכריע מחדש רק כש-`COUNT(*)>1`. לניק שכל ערכיו מהסריקה
  (מקור אחד) התצוגה התרוקנה בזמן ש-`field_values` המשיך להחזיק את הערך — 50 ניקים
  מורחקים הפסיקו להיספר ב-`get_stats` ולהיצבע כמורחקים. עכשיו מכריע לכל ניק שנשאר לו
  ערך כלשהו.
- `update_nick` כתב **את כל** העמודות עם `data.get(f,'')`, וטופס העריכה לא שולח
  `scraped_email`/`scraped_real_name` — כך שכל שמירה מחקה אותם, ומאז `email != scraped_email`
  הפך כל ניק שנערך ל"ניק עם מידע" (ושינה את ספירות הייצוא). עכשיו נוגעים רק במפתחות
  שנשלחו; מפתח קיים עם ערך ריק עדיין מנקה.
- `reset_columns` ניקה רק את ה-cache. מי שביקש למחוק טלפונים מהמאגר קיבל תצוגה נקייה
  והערכים חזרו בהכרעה הבאה. עכשיו מוחק גם `field_values`/`shelved_values`/`field_history`.
- `bulk_append_text` רשם תחת "אני" בלי להכריע — ההערה נמחקה בפעולה הבאה.

**סל המחזור לא היה מלא:** `field_history` מוגדר `ON DELETE CASCADE` ולא נכלל בצילום,
כך ש"מחק → בטל" מחק את ציר הזמן לתמיד. נוסף לצילום ולשחזור. בנוסף, אחרי שחזור רצה
הכרעה מחדש — ערך שמקורו נמחק בינתיים לא חוזר, וקודם ה-cache המשיך להציג אותו.

**`remove_identity` הוציא את הניק הלא נכון.** ה-✕ מוצג ליד כל *חבר אחר* בקבוצה, אבל
הקוד הוציא את הניק ה**פתוח** מכל הקבוצה — הרשימה נראתה כאילו נמחקה כולה. עכשיו יוצא מי
שלחצו עליו. (הקבוצה היא סגור טרנזיטיבי מלא, ולכן מחיקת זוג בודד לא הייתה מנתקת דבר.)

**רשת:**
- **עוגייה**: ההדרכה בממשק מבקשת להעתיק את *הערך* (`s%3A...`), אבל כותרת `Cookie` חייבת
  `שם=ערך`. `scraper.py` שלח את הערך כמו שהוא — הפורום התייחס אלינו כאורח, וסנכרון מול
  פורום שדורש התחברות פשוט לא הזדהה. `normalize_cookie()` מוסיף `express.sid=`/`_t=` לפי
  הפלטפורמה. (`chazonishnik.py`/`stinknik.py` כבר עשו זאת נכון — הבאג היה בסורק בלבד.)
- **`pagination.pageCount` של NodeBB**: CLAUDE.md כבר תיעד שהוא משקר בנתיב הפוסטים,
  אבל `_scrape_nodebb` המשיך לסמוך עליו ל-`/api/users` (`range(2, pageCount+1)`). התקנה
  שמחזירה `pageCount:1` נסרקה עמוד אחד ודווחה כ"הושלמה". עכשיו ממשיכים עד עמוד ריק כמו
  במסלול Discourse, עם `HARD_PAGE_CAP=4000`. **כלל: לא לסמוך על pageCount בשום נתיב.**
- **`max_pages`** סימן `limited` ומדווח "נעצרה לפי ההגבלה שהגדרת" במקום "הושלמה".
- **Discourse**: `str(x or "")` הפך מוניטין 0 ל-`""`, והמיזוג בלע אותו כאילו לא נסרק —
  מוניטין שירד ל-0 נשאר תקוע. עבר ל-`_num_str`.
- **נימוס**: Chazonishnik ירה 12 בקשות במקביל בלי כל השהיה (הסורק ממתין 0.6s ו-Stinknik
  0.4s). ירד ל-4 עובדים עם `DETAIL_DELAY=0.15`.

**יעילות — הממצא הגדול:** pywebview מריץ **כל קריאת גשר ב-thread חדש**
(`js_bridge_call` → `Thread(target=_call)`), ולכן החיבור ה-thread-local שנוסף ב-0.8.3
מעולם לא שרד בין קריאות: כל `api(...)` שילם connect + PRAGMA מחדש. נוספה **בריכת
חיבורים** (`_pool` + `_ConnHolder` שמחזיר את החיבור כשה-thread מת, `check_same_thread=False`).
נמדד: **3.40ms → 0.77ms לקריאת גשר**. בנוסף `PRAGMA busy_timeout=15000` — כתיבה בזמן
סריקה/ייבוא ממתינה במקום ליפול מיד על "database is locked" באמצע רצף כתיבות.

**עוד:**
- ניקוי `uid:` החד-פעמי רץ בכל הפעלה כ-`LIKE 'uid:%'` על כל 90k הניקים. מגודר בדגל.
- אינדקס `idx_nicks_forum_username` — החיפוש החם של הייבוא.
- ייבוא: קובץ שמכיל את אותו `(פורום, שם משתמש)` פעמיים יצר שני ניקים (סריקה עתידית
  מתאימה רק לאחד). `exported_fields` מוצלב מול `_NICK_FIELDS` — שם שדה מומצא בקובץ לא
  מזהם את `field_values`.
- `_busy()` אחד ב-`class API`: איפוס, מחיקה גדולה (מעל 50) ופעולות מקור לא רצות באמצע
  סריקה/ייבוא. עותקי `.before-restore-*` (88MB כל אחד) נשמרים 3 אחרונים בלבד.
- פעולות מרובות מדלגות על ניקים שנמחקו במקום ליפול על שגיאת FK גולמית.
- צד לקוח: סינון מתקדם החזיר את הגלילה לראש (החלון הווירטואלי הציג טבלה ריקה),
  ניקה בחירה תלויה (`selectedId`), ומונה "עם מידע" מציג `—` במקום 0 כשהסינון לא מחשב
  `has_info`. `hiddenColsSet()` נבנה פעם אחת לחלון במקום לכל שורה בכל פריים.

**מה נבדק ונדחה** — ההדרכה של Chazonishnik/Stinknik לעוגייה תקינה (הם מנרמלים בעצמם).

## מה נוסף ב-0.8.5 (2026-09-03) — ההצעות מניתוח הפערים

**ליבת המוצר — "מי זה מי":**
- **הצעות זהות אוטומטיות** (`suggest_identities`): GROUP BY על טלפון מנורמל / מייל / real_name / full_name (לא self-join — O(n) ולא O(n²)); מדלג על קבוצות שכולן כבר מקושרות (union-find דרך `_identity_groups_map`), על קבוצות באותו פורום בלבד, ועל זוגות שנדחו (`identity_dismissed`). UI: "🔗 הצעות זהות" עם קישור/דחייה בלחיצה.
- **פעולות מרובות**: `bulk_link_identities`, `bulk_move_forum` (bulk_update_field חוסם forum במכוון), `bulk_append_text` (מוסיף שורה להערות במקום לדרוס, ורושם תחת מקור "אני").

**תובנות:**
- **`field_history`** — טבלה חדשה; הרישום נעשה ב-`_resolve_fields_conn` (choke point יחיד לכל שינוי ערך מנצח), רק ל-`_HISTORY_FIELDS` (status/real_name/full_name/phone/email/address/groups — לא מוניטין/post_count שמשתנים כל הזמן). `_resolve_fields_bulk` **לא** רושם היסטוריה (פעולות מקור נוגעות במאות אלפי שורות). מוצג כציר זמן בדיאלוג הניק.
- **`scan_runs` + `scan_changes`** — `merge_scraped_users(..., run_id)` רושם ניקים חדשים ושינויים משמעותיים; `start_scan_run`/`finish_scan_run` ב-main. UI: "🕒 יומן סריקות" + toast עם "📋 מה השתנה" אחרי סריקה, כולל הדגשת הרחקות.
- **`get_stats`** — סה"כ/לפי פורום/מורחקים/עם מידע/זהויות/7 ימים אחרונים + קבוצות נפוצות. UI: "📊 סטטיסטיקות".

**חיפוש ושדות:**
- **חיפוש סלחני**: FTS הוא prefix-only ולכן "כהן" לא מצא "משהכהן". `_search_where(..., fuzzy=True)` מוסיף LIKE %term% — **רק** כשהחיפוש הרגיל החזיר פחות מ-5 תוצאות (סריקה מלאה יקרה).
- **`last_seen`** כשדה אמיתי (עמודה, סינון, ייצוא) — ממופה מ-`lastonline` ב-NodeBB ומ-`last_seen_at` ב-Discourse; קודם היה קבור כטקסט בתוך extra_info.
- **סינונים שמורים** (`saved_filters` ב-settings כ-JSON) — שמירה בשם והחלה בלחיצה מסרגל הסינון.

## שני באגים שבנימין דיווח (0.8.4) — שורש וסטטוס

1. **"שני תהליכים במקביל — Chazonishnik נעלם כשהפעלתי Stinknik"**: שלושת הבאנרים הצפים היו ממוקמים בדיוק באותו מקום (`bottom:16px;right:16px`), כך שהשני צייר על הראשון. **תוקן**: כולם בתוך `#bg-tasks` (flex column) ונערמים. שים לב שהמוניטורים עצמם (`_chzPoll`/`_stinkPoll`/`_scrapePoll`) תמיד היו עצמאיים — זו הייתה בעיה ויזואלית בלבד, והניתוח כן רץ ברקע.
2. **"Stinknik סרק רק אחוז מזערי מהפוסטים של לומדעס"**: נבדק מול השרת החי — הקוד **כן** עובר את כל העמודים (3,177 פוסטים מתוך 3,476 postcount, ~128 שניות, ללא עוגייה). שתי סיבות אמיתיות לפער שהמשתמש ראה: (א) בגרסה המשוחררת `_get_json` היה **בלי ריטריי**, וכל תקלת רשת/429 באמצע גרמה ל-`break` שקט שדווח כ"הושלם ✓" (תוקן ב-0.8.4 עם ריטריי + כיבוד Retry-After); (ב) ~300 פוסטים הם בקטגוריות שדורשות התחברות. **תוקן**: הסריקה מדווחת כעת בכנות — `postcount`, `partial`, `stopped_early`, `limited` חוזרים מ-`analyze_dislikes`/`analyze_user`, ה-toast אומר "נסרקו X מתוך Y" ומבחין בין "נעצר בגלל תקלת רשת" ל"השאר דורש התחברות", והדוח עצמו מציג "פוסטים שנסרקו מתוך Y". **לקח: אל תסמוך על `pagination.pageCount` של NodeBB בנתיב `/api/user/{slug}/posts` — הוא מחזיר 1 גם כשיש 170 עמודים; יש לעמוד עד עמוד ריק.**

## מה נוסף ב-0.8.4 (2026-09-02) — "חסרים הרבה דברים"

**מקור**: ניתוח פערי-מוצר (5 סוכנים, 38 הצעות; הרשימה המלאה ב-scratchpad של הסשן) + כל הפריטים שנותרו פתוחים מ-0.8.3.

**סל מחזור (`trash_nicks`)**: `delete_nicks` מצלם לכל ניק payload JSON מלא (השורה, contacts, identities, field_values, shelved, conflicts) ורק אז מוחק; מחזיר `{deleted, batch_id}`. `restore_trash(batch_id)` מחזיר עם **אותו id** (AUTOINCREMENT מבטיח שלא נוצל), מדלג על ניק שנוצר מחדש בינתיים, משחזר זהויות רק כששני הצדדים קיימים, וערכי מקורות רק אם המקור עדיין קיים. purge של 30 יום ב-`init_db`. ב-JS: `toast(msg,type,{actionLabel,onAction})` → "↩ בטל", ודיאלוג `openTrash()`.

**חיפוש**: `_search_where()` — FTS/LIKE על עמודות הניק **וגם** `nick_contacts.value`, **וגם** התאמת טלפון מנורמל (`_phone_norm_sql`, ווריאנטים 0…/972…). `search_nicks_for_lookup` באותה רוח.

**ממשק**: קישורי קשר (`contactMenu` — wa.me/tel:/mailto:/העתקה; `.contact-link[data-cval]` בהאצלה); `copy_to_clipboard` (ctypes, CF_UNICODETEXT — `navigator.clipboard` לא אמין ב-WebView); העתקת נבחרים כ-TSV ופרופיל מאוחד; `export_csv(mode, ids)` (utf-8-sig, `="0501…"` לאפסים מובילים) + `export_data(mode, ids)` עם מצבי `selected`/תצוגה; מקלדת (Ctrl+F, "/", Enter, חיצים, Delete, Esc); זיכרון `sort`/`last_search` ב-display settings; `updateSortIcons()`.

**תשתית**: ייבוא ב-thread (`_import_state`, `get_import_progress`, `runImport()`); פעולות מקור ב-thread (`_source_state`, `_run_source_op`, `waitSourceOp()`); `set_sync_settings`/`set_forum_io_flags`/`apply_import_conflicts` מרוכזים; `start_scrape_all(..., only_forums)` + `showSkippedForums()`; `last_scrape_<forum>` ב-settings + `get_last_scrapes`; `checkpoint()` בסגירת החלון + `journal_size_limit`; `db_health()`/`vacuum()`/`open_data_folder`/`open_log`; `backup_to`/`validate_backup`/`restore_from` (עותק בטיחות `.before-restore-*`); `_single_instance_or_exit` (mutex) ו-`_msgbox` בעברית ב-init_db כושל; עדכון עצמי ממתין לפי PID וכותב `update-failed.txt` (`consume_update_failure`); ריטריי ב-`_get_json` של Chazonishnik/Stinknik; חלון וירטואלי לכרטיסים (`measureCardsLayout`, `cardsSpacer`); אונבורדינג כשאין פורומים.

**מהניתוח — עדיין לא מומש (לפי ערך)**: הצעות זהות אוטומטיות (real_name/phone זהים בין פורומים); חיפוש תת-מחרוזת/סלחני בעברית; ציר זמן/היסטוריה לניק (field_values דורס created_at); דוח "מה השתנה בסריקה" + התראה על הרחקות; לוח סטטיסטיקות; ייבוא CSV; אנשי קשר וזהויות בפורמט .tiknick (גרסה 3); תצוגה מקדימה לייבוא; פעולות מרובות (קישור זהויות/העברת פורום/תיוג); סינונים שמורים; סריקה מתוזמנת; גיבוי אוטומטי יומי + לפני פעולות הרסניות; רוחב/סדר עמודות; פרופיל להדפסה; מפת זהויות.

## מה נוסף ב-0.8.3 (2026-09-02) — סריקת עומק

**מקור**: ביקורת רב-ממדית (6 סוכני קריאה-בלבד, 79 ממצאים) + מדידות על מאגר סינתטי של 20-40 אלף ניקים ועל ה-DB האמיתי של בנימין (88MB, 90k ניקים).

**ביצועים (הכי חשוב לזכור):**
- `get_connection()` הוא **thread-local ונשמר** (`_local`); מתחלף אוטומטית כש-`DB_PATH` משתנה. `with get_connection() as conn:` עדיין מבצע commit בסוף הבלוק — אבל עכשיו על חיבור משותף, כך שבלוקים מקוננים באותו thread חולקים טרנזקציה. פתיחת חיבור חדש לכל קריאה עלתה ~7ms.
- **רשימות (`get_all_nicks`/`filter_*`) לא מחזירות `avatar_image`** — רק `has_avatar`; התמונה נטענת ב-`get_avatars(ids)` רק לשורות המוצגות (`hydrateAvatars()` ב-JS, cache ב-`S.avatarCache`). `extra_info` נחתך ל-300 ברשימות בלבד (`_list_cols_sql`). זה הוריד את המטען לגשר מ-22.7MB ל-13MB על 20k.
- **אינדקס `idx_fv_nick_source(nick_id, source_id)` הוא קריטי**: בלעדיו המתכנן בחר ב-`idx_fv_source` ומקור הסריקה מחזיק כמעט את כל השורות → סריקה מלאה לכל משתמש (464ms/משתמש על ה-DB האמיתי). `PRAGMA optimize` רץ ב-`init_db`.
- `merge_scraped_users` שולף את הניקים הקיימים ואת ערכי הסריקה שלהם ב-**2 שאילתות לעמוד** (`_chunks` של 400), לא לכל משתמש.
- `_resolve_fields_conn` **מדלג על UPDATE כשהמנצח לא השתנה** (כל UPDATE על nicks משכתב את שורת ה-FTS). `_resolve_fields_bulk(conn, {nick:[fields]})` — 2 שאילתות ל-400 ניקים, משמש את `update_source`/`delete_source`.
- `import_data` כולו בחיבור אחד: טעינת קיימים במנות, `_upsert_field_value` + `_resolve_fields_conn` פעם אחת לניק.
- `delete_nicks`/`bulk_update_field` במנות של 400 (מגבלת פרמטרים של SQLite).
- `count_export_modes` סופר ב-SQL (`_HAS_INFO_SQL`/`_MY_INFO_SQL` — מקור אמת יחיד לתנאים).
- JS: `renderCards()` רק כש-`DISPLAY.view==='cards'`; `HE_COLLATOR` במקום `localeCompare` וללא מיון כש-`sortCol==='has_info'`; debounce+token ל-`applyFieldFilter` (מחזיר Promise), לתיוג `@` ולחיפוש זהויות; טולטיפים עם `ttBegin/ttValid`.

**אבטחה (RCE chain שנסגר):**
- דוחות Chazonishnik/Stinknik: iframe עם `sandbox="allow-scripts allow-popups"` (בלי allow-same-origin → אין גישה ל-`window.parent.pywebview.api`) + בריחה מלאה בתבניות (`_esc`, `_json_for_script` שמנטרל `</script>`).
- `openModal` מבצע `esc(title)`; `esc()` מנטרל גם `'`; `safeUrl()` לכל href/src ממקור פורום (רק http(s)/data:image); `applyAvatar` מאמת URL.
- handlers מוטבעים שנבנו משמות פורומים הוחלפו ב-`data-*` + האזנה מואצלת (`.known-add`, `.fmap-select`) — בריחת HTML לא מגינה על מחרוזת JS בתוך מאפיין.
- `download_update` מקבל רק https מ-github.com / githubusercontent.com; `_looks_like_inno_setup` מאמת התאמת סוג לפני `apply_update`.

**נכונות/יציבות:** `delete_nick`/`delete_nicks`/`bulk_update_field` מחזירים `{ok,error}`; ה-JS בודק ולא מדווח הצלחה על כישלון. `start_scrape_all`/`sync_selected_online` עם try/finally (אחרת `running` נתקע לנצח). הסורק סופר `failed_pages` ועוצר אחרי 5 כישלונות רצופים; ה-UI מציג "הסתיימה חלקית". `_map_user`: `_num_str` שומר 0 (ירידת מוניטין ל-0 מתעדכנת), תקרות ל-extra_info; במיזוג — הרחקה שבוטלה מנוקה ל"פעיל" רק אם הסריקה הקודמת רשמה "מורחק".

**UX:** מודאלים עם `opts.id`/`dismissable`; `_currentModalId` — מוניטורי הרקע סוגרים רק את החלון שלהם ושומרים `_lastReport` ("📄 הדוח האחרון"); דיאלוג הניק לא נסגר מרקע; `renderEmptyState` לפי חיפוש; `relativeTime` בעברית תקינה; `dir="ltr"` לשדות URL/מייל/מוניטין; `user-select:text` למשטחי ערכים; שם עוגייה לפי פלטפורמה (`#sync-cookie-name`); "סרוק הכל" משתמש **רק** בעוגיות שמורות פר-פורום (פרטיות).

**עדיין פתוח (מהביקורת, לא טופל):** כרטיסים גדלים בלי גבול בגלילה; ייבוא עדיין חוסם את ה-UI (מהיר, אבל בלי פס התקדמות); `update_source` ~10s על 40k ניקים (עם משוב, לא ברקע); עוגיות בטקסט גלוי ב-SQLite (DPAPI אפשרי); אין אימות חתימה על קובץ עדכון (רק allow-list של מארח + בדיקת סוג); רשימת הפורומים שדולגו ב"סרוק הכל" לא מוצגת בפירוט; הגדרות סנכרון מערבבות שמירה-מיידית ושמירה-בכפתור.

## מה נוסף ב-0.8.2 (2026-09-01)

- **סריקת Discourse** לצד NodeBB: `scraper.detect_platform` + dispatch ב-`scrape_forum`. פלטפורמות ללא API ציבורי (XenForo/phpBB/custom) מרימות `ScrapeError` ידידותית ולא נסרקות. `check_forum` מחזיר `platform` ושומר אותו לפורום.
- **רשימת פורומים מורחבת** (KNOWN_FORUMS ~24): נוספו NodeBB — ימות המשיח (f2.freeivr.co.il), נטפרי (forum.netfree.link), חרדים נעייס; ופלטפורמות לא-סריקות עם `platform` + `profile_pattern` — לתורה/פרוג/אוצר התורה (xenforo), אוצר החכמה/אייוועלט (phpbb), בחדרי חרדים (custom). **מקור פלטפורמה**: מחקר אמת שנעשה ב-0.8.2 (ראו למטה).
- **עוגיות נשמרות לפי origin** (טבלה `forum_cookies`): `db.get/save_cookie_for_url`, `db._origin`. נשמרות אוטומטית בסריקה/Chazonishnik/Stinknik, משמשות שוב אוטומטית, ומוצגות מראש בדיאלוגים. `start_scrape_all`/`sync_selected` משתמשים בעוגייה השמורה לכל פורום.
- **תצוגת משתמש מאוחדת** (feature): `db.get_merged_profile(nick_id)` + `search_nicks_for_lookup` → כפתור "🔎 תצוגת משתמש", מרכז מידע מכל קבוצת הזהות.
- **מצבי ייצוא**: `export_data(mode)` + `count_export_modes` — all / has_info / my_info. דיאלוג בורר עם ספירות.
- **הגבלת כמות**: `max_posts` ל-Chazonishnik/Stinknik (זמן ריצה), `max_pages` בממשק הסנכרון.
- **גרסת אינסטולר** לצד ניידת: `installer.iss` (Inno Setup, per-user, ללא UAC) + `build_installer.bat` → `dist\TikNick-Setup.exe`. `_install_type()` מזהה לפי `install-type.txt` ליד ה-EXE (מותקן ע"י האינסטולר). עדכון עצמי בוחר נכס לפי סוג: ניידת→EXE, מותקנת→Setup (מריץ `/SILENT`). כלל שמות נכסי Release: שם המכיל "setup"/"install" = אינסטולר.
- **תיקון חלוניות צפות איטיות**: guard נגד polls חופפים + `_yieldPaint()` לפני `loadNicks` הכבד — הבאנר נעלם מיד.
- פלטפורמה+תבנית-פרופיל זורמות דרך `resolve_forum_data`/`add_forum`; `buildProfileUrl(forum,...)` ב-JS בונה לינק לפי פלטפורמה/תבנית.
- **אבטחה (ביקורת אדוורסרית)**: ערכים סרוקים (`nick_color`, `avatar_image`) הם קלט לא-בטוח מהפורום — כל הזרקה ל-innerHTML עוברת `esc()` (renderMergedProfile, דיאלוג הניק, כרטיסים). כלל: **לעולם לא לשרשר ערך שמקורו בפורום ל-innerHTML בלי esc**.
- תיקוני ביקורת: `stinknik._resolve_slug` מנסה וריאציות סלאג (שמות עם רווח נפתרים); `detect_platform` בודק Discourse לפני שמסיק NodeBB מ-401/403; עדכון עצמי בוחר נכס בהתאמה קפדנית (ניידת↔EXE, מותקנת↔Setup — בלי fallback חוצה-סוגים); `start_scrape` מאפס מצב רב-פורומי; `sync_selected` בחיבור DB אחד עם cache עוגיות.

## מחקר פלטפורמות הפורומים (2026-09-01, אימות חי)

| פורום | URL | פלטפורמה | סריקה? |
|---|---|---|---|
| מתמחים טופ / בינה / קהילה וכו' | (קיימים) | NodeBB | ✅ |
| ימות המשיח | f2.freeivr.co.il | NodeBB (13,576 משתמשים) | ✅ |
| נטפרי | forum.netfree.link | NodeBB (לעיתים במצב תחזוקה 503) | ✅ |
| חרדים נעייס | charedim-neyes.onrender.com | NodeBB (Render — נרדם, מתעורר לאט) | ✅ |
| לתורה / פרוג / אוצר התורה | tora-forum / prog.co.il / forum-otzar-hatorah | **XenForo** (REST דורש API key) | ❌ |
| אוצר החכמה / אייוועלט / אידטיש | forum.otzar.org / ivelt.com / yidtish | **phpBB** (memberlist HTML בלבד) | ❌ |
| בחדרי חרדים | forums.bhol.co.il | ASP ייחודי | ❌ |
| קדם (נשים) | kedemcenter.co.il | WordPress+BuddyBoss (יש `/wp-json/buddyboss/v1/members`) | לא נוסף |

**למה "לתורה" לא הסתנכרן**: הוא XenForo, וה-REST של XenForo דורש `XF-Api-Key`. אין דרך לספור/למשוך את כל המשתמשים בלי מפתח או session מאומת — לכן אין סריקה אוטומטית (מסומן במפורש בממשק).

## מה תוקן ב-0.8.1 (2026-09-01)

- **האצת סריקה פי ~400**: `merge_scraped_users` (עמוד = טרנזקציה אחת + זיהוי שינויים), `_resolve_fields_conn` (הכרעה מרובה ב-UPDATE אחד), `force_scraped_values` לסנכרון נבחרים. נבדק: 300 משתמשים × 9 שדות — ‎38.5s → ‎0.1s.
- **חיפוש**: full_name ו-address נוספו לחיפוש המהיר (FTS + LIKE); מיגרציית FTS אוטומטית ל-DB קיים.
- **זרימת ההתנגשויות המתה הוסרה**: אין עוד conflict_policy, פותר גלובלי, `apply_conflict`/`resolve_all_conflicts`. נשאר: `nick_conflicts` לצפייה/סגירה בדיאלוג הניק (legacy), ומדיניות ייבוא ידני (`import_manual_conflicts`).
- Chazonishnik: Chart.js מוטמע (אופליין), בורר פורום, עוגייה אופציונלית.
- `check_forum` מזהה דרישת התחברות דרך `AuthRequired` (הענף המת תוקן).
- ייבוא ידני: תוקן N+1 (‎snapshot אחד לניק).
- קוסמטיקה: footer מקבל גרסה מ-`get_app_version` בהפעלה (גם אופליין); הוסרו סימוני גרסה סטטיים מ-app.js/style.css/build.bat; `upload_to_github.bat` נמחק (remote ישן ומסוכן).

## פינות ידועות שנשארו

1. גיט: commit יחיד squashed; זהות `BeniaBot` / `b0554003794@gmail.com`.
2. `get_nicks` מחזיר את כל העמודות כולל `avatar_image` (base64) לכל הרשימה — כבד כשיש הרבה אווטארים; מועמד לעתיד (להחזיר בלי אווטאר ולהשלים lazy).
3. `import_data` עדיין פותח חיבור פר-ערך דרך `record_field_value` — עובד, אבל אפשר להעביר לאותו דפוס batching אם ייבואים גדולים יאטו.
4. הוראות העוגייה ב-Chazonishnik מזכירות את mitmachim כדוגמה קשיחה בטקסט ההדרכה.

## פקודות

```
python main.py        # הרצה מהמקור (או הפעל.bat)
build.bat             # EXE → dist\TikNick.exe
```

**בדיקות:** `python tests/run_all.py` (או `run_tests.bat`) — 17 חבילות (כולל `test_*.js` ב-node), ~80 בדיקות על database/scraper/API (DB זמני, בלי רשת, ~1 דקה). `tests/bench_scale.py` ו-`tests/bench_critical.py` הם בנצ'מרקים על מאגר סינתטי גדול (20-40k ניקים) — להריץ לפני/אחרי כל שינוי במסלולי ה-DB החמים. אין linter; `node --check web/app.js` + `python -m py_compile` לתחביר. בדיקות UI: `python tests/harness.py` מגיש את הממשק מול גשר מזויף לבדיקה בדפדפן.

## עקרונות עבודה

- כל הטקסט למשתמש בעברית; הקוד והלוגים באנגלית/מעורב כמו הקיים.
- הוספת יכולת = method ב-`class API` + קריאת `api('...')` ב-app.js. אין framework — vanilla הכול.
- פרטיות היא ערך מוצר: private_notes/avatar_image לא מסונכרנים בברירת מחדל, סריקה לא נוגעת בשדות אישיים, הכול מקומי.
- אל תשבור את פורמט `.tiknick` בלי bump ל-version בפורמט + תאימות לאחור.
- זהירות ב-SQL דינמי: שמות שדות רק מרשימות לבנות (`_NICK_FIELDS`, `_FILTERABLE_KEYS`) — לשמור על הדפוס.
