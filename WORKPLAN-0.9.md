# WORKPLAN 0.9 — מצב העבודה, לקריאה בתחילת סשן

> עודכן 2026-09-07. **קרא את `CLAUDE.md` קודם** (מקור האמת לתכנון), ואז את זה.
> כשגרסה 0.9.0 משוחררת — למחוק את הקובץ ולהעביר את התוכן ל-`CLAUDE.md`.

## איפה הכול עומד

`APP_VERSION` = **0.8.23**. היעד: **0.9.0**.
מדיניות "להישאר ב-0.8.x" ב-CLAUDE.md **בוטלה** — בנימין ביקש 0.9 במפורש.

**הכול מקומט ונדחף.** `git log --oneline` על origin/main:

```
c63f8c9  v0.9 improvements: "does not contain", the forum that vanished, the mail button
800a45f  Wave 2, group 7: export scope + the last Hebrew leaks
bcf2cd4  Wave 2, groups 5-6: the client, and backups that vanished
739e091  Wave 2, group 4: the scraper stops claiming success it did not have
5610a14  Wave 2, group 3: two more cache-vs-engine splits, a merge that never converged
b1e6026  Wave 2, group 2: data loss, a broken update, and a cookie that leaked
766e378  Wave 2, group 1: the two criticals and the template-substitution hole
566e23f  Fix "שלח מייל" — @tsoolgee (PR #4, מוזג ב-rebase)
ec490b1  Wave 1 of the full bug hunt: 10 findings fixed
```

בדיקות: **21 חבילות, הכול ירוק** (`python tests/run_all.py`).

---

## מה נשאר לעשות

### 1. פיצ'ר המתנה 🎁 — ההחלטה תלויה ב-workflow שרץ
פאנל שיפוט: חמש הצעות מזוויות שונות, ארבעה שופטים מנוגדים, וסינתזה.

```
runId:      wf_d9cf867d-2b0
scriptPath: C:\Users\בנימין\.claude\projects\D--Claude\0c85c0f0-e108-4518-a863-9a0e9268ca95\workflows\scripts\tiknick-gift-feature-wf_d9cf867d-2b0.js
journal:    C:\Users\בנימין\.claude\projects\D--Claude\0c85c0f0-e108-4518-a863-9a0e9268ca95\subagents\workflows\wf_d9cf867d-2b0\journal.jsonl
```

**אם הסשן נפל לפני שהתוצאה נקראה**: לקרוא את `journal.jsonl` (שורת
`{"type":"result"}` לכל סוכן שהספיק), ואם צריך לחדש:

```
Workflow({scriptPath: "<למעלה>", resumeFromRunId: "wf_d9cf867d-2b0"})
```

התוצאה מחזירה `verdict` עם `winner_id`, `build_order`, `hebrew_strings`,
`tests`, `perf_check` ו-`biggest_risk` — מספיק כדי ליישם ישירות.
**אם אי אפשר לשחזר** — לבחור לבד, לפי האילוצים שנכתבו בסקריפט: בלי כפתור
חדש בתפריט, בלי תלות חדשה, עובד ב-90 אלף ניקים, ונבנה ונבדק בסשן אחד.

### 2. שחרור 0.9.0
1. `APP_VERSION = "0.9.0"` ב-`main.py`.
2. `python tools/sync_version.py` — מסנכרן `version_info.txt` ו-`installer.iss`.
   (`build.bat` מריץ את זה לבד, והבדיקות נכשלות על סטייה.)
3. פריט ל-`changelog.json` בראש הרשימה, עם תיוגי `major`/`feature`/`fix`.
4. לעדכן את `CLAUDE.md`: סעיף 0.9.0, ומחיקת שורת מדיניות הגרסאות הישנה.
5. commit + push → `build.bat` → Release עם tag **`v0.9.0`** ושני נכסים
   (`dist\TikNick.exe` + `dist\TikNick-Setup.exe`).

**כלל זהב: `APP_VERSION` == ה-tag בלי v.** אחרת בדיקת העדכונים נשברת לכולם.

### 3. אולי: לענות ל-@tsoolgee ב-PR #4
בנימין עוד לא אישר. אם יאשר — תודה, מה נלקח, ומה הוכלל ולמה (הקהל מאחורי
סינון, ולכן לא מקבעים ספק דואר אחד).

---

## מה כבר נעשה — הסריקה המלאה

**44 ממצאים בסך הכול** (10 בגל 1, 34 בגל 2), כולם מיושמים ומאומתים בהרצה.
121 בדיקות אימות + `tests/test_audit_09.py`. אין צורך לחזור אליהם.

### גל 1 — קומיט `ec490b1`
NET-1 (ה-Request שנבנה מחוץ ללולאת הריטריי — עוגייה בטקסט גלוי), NET-2/3,
UPD-1/3/4, תוויות הגיליון להדפסה, יום־בשבוע כמחרוזת מתורגמת, ושני באגים
במיזוג הכפולים.

### גל 2 — שש עדשות, מפריך לכל אחת
| קומיט | מה |
|---|---|
| `766e378` | **קריטי**: `avatar_image` בלי `esc` בשדה חבוי — הזרקה מקובץ ייבוא שרצה במסמך הראשי. **קריטי**: ה-`<option>` של הסטטוס בלי `value`, ולכן באנגלית נשמר "Banned" וכל בדיקת הרחקה בתוכנה (השוואה למחרוזת עברית) הפסיקה לזהות אותו. וכותרת פוסט שמכילה `__BASE_URL__` הרסה דוח שלם בשקט. |
| `b1e6026` | הורדת עדכון קטועה הותקנה ומחקה את העותק הטוב; סל המחזור מחק צילומים שדילג עליהם; פורום/שם משתמש כמתגי סנכרון; עוגייה לא מנורמלת בשלושה מסלולים; **עוגייה שדלפה למארח אחר בהפניה**. |
| `5610a14` | עוד שני מקומות שכותבים ל-cache בלי הכרעה; הרחקה שבוטלה שאי אפשר לנקות; מיזוג כפולים שלא התכנס; יומן הסריקות ששרד "מחק הכול". |
| `739e091` | `limited` שנזרק במסנן ההתקדמות; לולאת Discourse בלי בלם (+`logging` שלא היה מיובא כלל); "0 לייקים" שדווח כניתוח שהושלם; כשל בעמוד הראשון שדווח "לא נמצאו פוסטים". |
| `bcf2cd4` | גרירת עמודות שבורה ב-LTR; איפוס שדיווח הצלחה כשסורב; תרגום שנגע בנתונים ועמודת "עודכן" שלא תורגמה; שני חלונות בלי "↩ חזרה"; גיבוי שנעלם מהרשימה אחרי שחזור; שתי לחיצות על "גבה עכשיו". |
| `800a45f` | הייצוא שקרא את כל הטבלה; 14 הודעות פייתון בלי אנגלית. |

### השיפורים — קומיט `c63f8c9`
"לא מכיל" בסינון ובחיפוש (+ בריחת ג'וקרים ו-COALESCE, שני באגים שהיו קיימים
ב"מכיל"); הסרת תקרת ה-12 מבורר בדיקת החיבור; כפתור המייל שבודק אם יש מטפל
`mailto` רשום.

### מה נשאר מהרשימה של המשתמש ולא מומש
**פרוקסי רק אם הישיר נכשל, בזיכרון לכל פורום** — תוכנן ולא נבנה. העיצוב:
מצב רביעי לצד `system`/`off`/`manual`; ניסיון ישיר, וכשל **רשת** (לא 401/403,
שהם תשובות תקפות) → ניסיון דרך הפרוקסי; הזיכרון לכל origin בנפרד כמו
`forum_cookies`, **עם תפוגה** כדי שפורום שהיה למטה לרגע לא יינעל לתמיד.
הכלל שלא נחלש: עוגייה של פורום אחד לא נשלחת לאחר.

---

## כללי ברזל לסוכני ביקורת (חוזרים בכל גל)
קריאה בלבד · **אסור לגעת ב-`%APPDATA%\TikNick`** (המאגר האמיתי, 90k ניקים) ·
**אסור לשלוח בקשה לפורום אמיתי** · בלי סיבוב User-Agent ובלי עקיפת Cloudflare
(החלטת מוצר) · פורטים 5173/3000/4321/5178 חסומים במכונה.

## העדפות עבודה של בנימין
- **לדבר איתו בעברית.** קוד, קומיטים ולוגים באנגלית.
- קומיט נפרד לכל שינוי, מוגבל-נתיב.
- "תבדוק טוב, אל תסמוך עליו" — לאמת טענות של סוכנים בעצמי, **בהרצה**.
- כתיבה מפוסקת ואוורירית, לא בלוקים דחוסים.
- קהל חרדי: לא לקבע מותגים במסלול המוצר.

## מלכודות שחזרו על עצמן בסשן הזה
- **heredoc של bash בולע בקסלשים.** כל סקריפט תיקון עם regex או `\\` נכתב
  דרך כלי Write, לא דרך `<<'PY'`.
- סקריפט תיקון שה-`new` שלו מכיל את ה-`old` מוחל **פעמיים** בהרצה חוזרת.
  לכן `patch()` בודק `if a not in s and b in s: skip`, וגם `count(a) == 1`.
- להריץ את הקוד, לא לקרוא אותו: הבלם שהוספתי ללולאת Discourse מת מיד על
  `NameError` כי `scraper.py` לא ייבא `logging` — קריאה לא הייתה תופסת.
